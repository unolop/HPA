%File: paper.tex — AAAI 2026 Submission
\documentclass[letterpaper]{article}
\usepackage[submission]{aaai2026}
\usepackage{times}
\usepackage{helvet}
\usepackage{courier}
\usepackage[hyphens]{url}
\usepackage{graphicx}
\urlstyle{rm}
\def\UrlFont{\rm}
\usepackage{natbib}
\usepackage{caption}
\frenchspacing
\setlength{\pdfpagewidth}{8.5in}
\setlength{\pdfpageheight}{11in}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{multirow}

\graphicspath{
  {./}
  {figures/}
}

\pdfinfo{/TemplateVersion (2026.1)}

\setcounter{secnumdepth}{2}

\title{Human and Model Priors under Referential Underspecification in Visual Questions}

\author{Anonymous}
\affiliations{Anonymous Institution}

\begin{document}
\maketitle

% ─────────────────────────────────────────────────────────────────────────────
\begin{abstract}
Vision-Language Models (VLMs) can achieve non-trivial accuracy on visual
question answering (VQA) benchmarks without any visual input, suggesting
reliance on linguistic priors embedded in the question text. This is
typically treated as a training-data leakage artifact or shortcut
behavior, but humans facing questions under incomplete information also
rely on systematic cognitive heuristics---availability,
representativeness, and world knowledge---to produce structured rather
than random responses. Are model blind priors merely dataset leakage, or
do they reflect something closer to the human cognitive priors that
arise under the same conditions? Prior work has diagnosed this phenomenon
through benchmark filtering and counterfactual images, but the
\emph{structure} of these model priors---and whether they resemble the
human priors that arise under the same conditions---remains largely
unexplored. We address this gap by comparing blind responses from 37 models
across three architectural groups against a human blind-answer reference
($N{=}40$), measuring semantic agreement rather than ground-truth accuracy.
To analyze where human and model priors converge or diverge, we introduce an
\emph{operation--entity taxonomy} that categorizes each question by its
reasoning operation (count, identity, action, attribute, spatial) and entity
type (person, animal, object, food, place). A counterintuitive finding
emerges: the model group most aligned with human blind priors is not the full
VLM but the \emph{backbone decoder}---the same weights with the vision encoder
bypassed. Image-condition ablations show that on uninformative input, the
vision encoder actively modulates the language model's output distribution
rather than cleanly withdrawing. Progressive reformulations that strip
content-noun anchors reveal lexical-anchor dependence: model agreement drops
monotonically while the human--human ceiling barely moves. The
operation--entity decomposition further shows that alignment varies
systematically by reasoning type, with identity questions most sensitive to
anchor removal and count questions showing persistent human--model divergence
in answer distributions.
\end{abstract}

% ─────────────────────────────────────────────────────────────────────────────
\section{Introduction}

Vision-Language Models (VLMs) achieve impressive performance on visual question
answering (VQA) benchmarks \citep{antol2015vqa,goyal2016making}. Yet prior
work has shown that text-only models can achieve non-trivial accuracy on VQA
without any visual input \citep{jabri2016revisiting,zhang2016yin}, and recent
benchmark audits confirm that language-only signals remain exploitable in
modern multimodal evaluations \citep{chen2024are,lee2024vlind}.
Analogous to hypothesis-only baselines in natural language inference
\citep{poliak2018hypothesis}, where models can predict entailment from the
hypothesis alone without seeing the premise, VLMs can exploit shortcut priors
carried by the question text to produce plausible answers without grounding
them in the image. This raises a broader question beyond benchmark accuracy:
when visual grounding is unavailable, what priors drive model answers, and to
what extent are those priors shared with human linguistic expectations? In
this paper, we investigate this question by comparing blind model responses
against a human reference, revealing that the vision encoder's behavior on
uninformative input plays a key role in shaping how model priors diverge from
human ones.

Prior work on VQA has largely approached this issue through the lens of
dataset bias and shortcut learning. Early work showed that text-only models
can answer many VQA questions correctly without visual input
\citep{jabri2016revisiting,zhang2016yin}, motivating the complementary-image
construction of VQA~v2.0 \citep{goyal2016making}. More recent evaluation
audits show that substantial language-only signal remains available even in
modern multimodal benchmarks \citep{chen2024are,lee2024vlind}. In our own
blind VQA setting, modern VLMs still answer 37--54\% of questions correctly
without seeing any image. However, most prior work evaluates this phenomenon
primarily in terms of benchmark accuracy. Much less is known about the
\emph{structure} of the underlying answer priors themselves, including which
lexical anchors allow models to shortcut image grounding.

At the same time, research in cognitive psychology has long established that
humans facing decisions under uncertainty rely on systematic heuristics---such
as availability and representativeness \citep{tversky1974judgment}---rather
than responding randomly. When visual evidence is missing, these heuristics
produce structured prior-driven responses: predictable default answers that
reflect world knowledge, prototypical associations, and frequency-based
expectations \citep{kahneman2011thinking}. This makes human blind answers a
meaningful reference point, not mere noise. Recent work on human--AI alignment
argues that model behavior should be evaluated against human response
distributions rather than only against task labels
\citep{lan2025mind,bao2026imagination}. \citet{kwon2025whatllms} show that
LLMs exhibit distinct intrinsic generative biases even under minimal
prompts, with different model families favoring systematically different
topical and structural defaults. Under ambiguous or underspecified
inputs, humans show structured patterns of agreement and disagreement that
reveal latent expectations about plausible interpretations, whereas
contemporary models often collapse toward narrow modal regions of response
space \citep{bao2026imagination}. In our setting, this motivates treating
blind VQA answers not just as right or wrong guesses, but as distributions
over plausible interpretations: some questions induce strong shared human
priors, while others open up pluralistic answer regions once lexical anchors
are weakened. The question is not whether such priors exist, but which priors
remain stable, which collapse under underspecification, and where model priors
diverge from human ones.

In this work, we study human and model priors under referential
underspecification in visual questions. Starting from VQA questions, we
construct progressively weaker variants that degrade object-denoting lexical
anchors while preserving grammatical structure, creating a controlled
degradation axis from fully specified questions to increasingly
underspecified formulations. This lets us probe where humans and models rely
on those anchors, how much blind answering survives when the anchors are
weakened, and where linguistic priors begin to diverge from grounded visual
reasoning. We first characterize how human answer distributions change along
this axis, identifying question categories that maintain strong consensus and
categories whose answers rapidly diversify under ambiguity. We then compare
these patterns against a diverse set of vision-language models, backbone
decoders, and language-only models to investigate where model priors align
with human priors and where they diverge.

This control-variant ladder also functions as a pronominalization test for
visual grounding. If a model continues to perform well after content-noun
anchors have been weakened or replaced by pronouns, then its blind behavior is
more likely to be carried by shortcut priors than by robust grounding cues.

Our contribution lies in the combination of three methodological tools.
First, we recruit 40 participants to answer VQA
questions under the same blind condition as the models, providing a human
reference for what linguistically plausible prior-driven answers look like.
Second, we introduce an \emph{operation--entity taxonomy} that categorizes
each question along two semantic dimensions---the reasoning operation
required (count, identity, action, attribute, spatial, text) and the entity
type mentioned (person, animal, object, food, place)---enabling diagnostics
at a finer grain than the VQA dataset's original question-type annotations.
Third, we construct progressively weaker question reformulations that strip
content-noun anchors from the original text, creating a controlled
degradation axis that isolates which lexical elements carry prior information.

Together, these tools reveal several findings across 37 models from four
architectural groups. Human answer priors without visual evidence turn out
to be selectively structured: consensus depends on what kind of reasoning
the question requires, not what objects it mentions
(Figure~\ref{fig:human_degradation}). On the model side, the group whose
answers most closely resemble those of humans is not the full VLM but the
backbone decoder---the same weights with the vision encoder bypassed
(Figure~\ref{fig:scale}). This gap persists across model scales, suggesting
that architecture, rather than capacity, determines how human-like the
resulting priors are. As content-noun anchors are progressively removed,
model--human agreement drops monotonically while the human--human ceiling
barely moves (Figure~\ref{fig:variants}), confirming that models depend
heavily on specific lexical cues that humans do not require.
Image-condition ablations (blank, gray, noise, white) further show that the
vision encoder actively modulates the language model's output even on
uninformative input (Table~\ref{tab:image_ablation_main}); the backbone
decoder, free from this modulation, falls back on the pure linguistic
prior---which turns out to be the most human-like. Finally, our taxonomy
reveals where human--model alignment holds and where it breaks: counting
questions show the largest persistent gap despite high human consensus,
with models collapsing to generic small numbers while humans distribute
more broadly (Figure~\ref{fig:op_hm}).

% ─────────────────────────────────────────────────────────────────────────────
\section{Related Work}

\paragraph{Question-Only Reasoning, Shortcut Learning, and Ambiguity in Visual Questions.}
Early analyses of VQA benchmarks revealed that text-only models could achieve
non-trivial accuracy without visual input, exposing exploitable
question--answer regularities in the data \citep{jabri2016revisiting}.
Subsequent studies identified systematic answer biases
\citep{zhang2016yin} and showed that many VQA systems rely on language
shortcuts rather than visual grounding \citep{agrawal2018dont}, motivating
debiasing and counterfactual intervention methods
\citep{niu2021counterfactual}. As VLMs have scaled in both architecture and
training data, these concerns have resurfaced: recent benchmark audits show
that language-only signals remain prevalent even in modern multimodal
evaluations \citep{chen2024are,lee2024vlind}. In parallel, recent work has examined how
humans and AI systems behave under ambiguity, underspecification, and
incomplete grounding. AmbigQA \citep{min2020ambigqa} demonstrated that many
questions admit multiple plausible answers and are better represented as
answer distributions rather than single gold labels. Hallucination and
shortcut learning have likewise been widely documented in vision-language
models \citep{ji2023survey,rohrbach2018object,xiao2021hallucination}. Prior
work shows that VLMs can exploit spurious visual or linguistic cues
\citep{han2024instinctive,ye2024mmspubench}, fail to maintain compositional
visual-linguistic bindings \citep{yuksekgonul2023when}, and answer benchmark
questions without requiring image evidence \citep{chen2024are,lee2024vlind}.
These benchmark audits diagnose \emph{whether} models rely on language
priors---\citet{chen2024are} filter for vision-indispensable items, while
\citet{lee2024vlind} test with counterfactual images---but do not ask what
the structure of those priors is or how it compares to human behavior.
We take a complementary approach: rather than filtering out prior-solvable
questions or manipulating images, we remove the image entirely and compare
the resulting model answer distributions against those of humans under the
same blind condition, using a semantic taxonomy that decomposes prior
structure by reasoning operation and entity type.

\paragraph{Human--Model Alignment and Answer Priors.}
A growing body of work evaluates AI systems relative to human judgments rather
than benchmark labels alone. Prior work argues for evaluating alignment
through similarities between human and model response distributions,
particularly in settings involving ambiguity and underspecification.
\citet{bao2026imagination} study human--AI agreement at the level of response
distributions rather than only top-1 labels. In the multimodal domain,
\citet{lan2025mind} compare VLM uncertainty against human uncertainty, while
\citet{lee2023visalign} study alignment between human and model visual
judgments. From the human side, dual-process accounts of judgment under
uncertainty emphasize heuristic reasoning through availability and
representativeness \citep{tversky1974judgment,kahneman2011thinking}, which
provides a useful cognitive backdrop for interpreting blind answers as
plausible guesses under incomplete grounding rather than arbitrary noise. Our
work extends this line of research to visual questions under controlled
referential degradation. Rather than focusing on perceptual judgments or
confidence calibration alone, we compare human and model answer
distributions as visual grounding is progressively weakened.

% \paragraph{Model Calibration and Overconfidence.}
% \citet{guo2017calibration} showed that modern neural networks are
% systematically overconfident---predicted probabilities do not reflect true
% correctness likelihoods. We find the same pathology in the generative VLM
% setting: blind outputs remain in a high token-confidence regime even for wrong
% committed answers, making this failure invisible to accuracy-only evaluation.
% \citet{kadavath2022language} showed LLMs can express calibrated uncertainty
% when asked to predict their own correctness; under our explicit no-image
% instruction, model confidence \emph{increases} instead---indicating committed
% hallucination rather than acknowledged uncertainty.

% ─────────────────────────────────────────────────────────────────────────────
\section{Experimental Setup}
\label{sec:setup}

\subsection{Models, Dataset, and Inference Conditions}

We evaluate VLMs from the Qwen3-VL \citep{bai2025qwen3vl}, LLaVA
\citep{liu2023visual,liu2024llavanext}, and InternVL \citep{zhu2025internvl3}
families, plus standalone LLM baselines (Qwen3 \citep{yang2025qwen3},
Qwen2.5-7B(-Instruct), Mistral-7B, Vicuna-7B/13B, Phi-3.5-mini).
All evaluations are zero-shot at temperature~0. Throughout the paper, we use
\emph{model family} for named lineages such as Qwen3-VL, InternVL, and
LLaVA, and \emph{model group} for the three evaluation categories below.
Models are organized into three groups:
\textbf{VLM} (full vision-language stack), \textbf{Backbone decoder} (same
checkpoint, vision encoder bypassed), \textbf{standalone LLM}, and
a 7--8B scale-matched subset is used for fair size-controlled comparisons.
Importantly, \textbf{Backbone decoder} runs are weight-identical to their paired
VLM checkpoints and differ only by removing image input at inference, whereas
\textbf{standalone LLM} runs use separate family-matched checkpoints (e.g.,
Mistral-7B as the standalone counterpart to LLaVA-Mistral, Qwen3-8B to InternVL-8B)
rather than decoders extracted from the VLM weights.
All inference and analysis runs were executed on a workstation with dual
NVIDIA TITAN RTX GPUs.

Our evaluation dataset is a subset of VQA v2.0 validation
split \citep{goyal2016making}, spanning yes/no, numerical, and open-ended
types. Human--model comparisons use 113 questions common to all qualifying
participants (\S\ref{sec:human_data}) across three controlled reformulations,
yielding 339 question--variant pairs.
The subset covers the full semantic range of the
pool, confirmed by t-SNE embedding visualization
(Appendix~\ref{app:tsne}).

Our experimental paradigm uses a blank 224$\times$224 black image in place of
the original visual input, together with the instruction \textit{``Note: No
images are provided. For each question, imagine an appropriate image exists
and answer based on the most common or universal scenario.''} This framing
elicits prior-driven answers while reducing refusal behavior. Because
abstention is also an informative aspect of model behavior, we additionally
evaluate blind answering without the instruction and report the resulting
abstention shifts as a supplementary robustness analysis
(Appendix~\ref{app:abstention_calibration}).

\subsection{Control Question Variants}
\label{sec:control}

To isolate the contribution of linguistic priors, we generate progressively
abstracted variants of each VQA question using GPT-4.1-mini under a constrained
JSON schema. The transformations systematically remove lexical information while
preserving the underlying question structure and reasoning operation. We create
four variants: (1) deictic markers and spatial references removed, (2) specific
object nouns replaced with a neutral placeholder, (3) object nouns generalized
to a higher-level semantic category (e.g., \emph{dog} $\rightarrow$
\emph{animal}), and (4) full pronominalization, in which content noun phrases
are replaced with minimal pronouns while retaining the original grammatical
frame. The complete prompt, schema, and transformation rules are provided in
Appendix~\ref{app:ctl}.

These variants define a controlled abstraction hierarchy that progressively
reduces entity-specific information while preserving the operation required to
answer the question. This allows us to test whether human and model responses
depend primarily on lexical cues or on broader semantic and commonsense priors.
For analysis, each question is additionally annotated along two orthogonal
dimensions: \emph{operation} (e.g., count, attribute, action, spatial,
identity) and \emph{entity} (e.g., person, animal, object, vehicle, food,
place). We use this two-axis taxonomy to separate \emph{what kind of reasoning}
the question demands from \emph{what semantic field} its lexical anchors evoke,
so that agreement patterns can be analyzed independently by operation and by
entity type rather than conflated in a single label space.

\subsection{Human Data Collection}
\label{sec:human_data}

40 participants were recruited via online university community forums under
an IRB-approved protocol. Participants answered text-only
explicit no-image blind-instruction questions in Korean or English; responses were translated and
normalized. Translation and normalization are applied uniformly before
scoring so that agreement metrics reflect semantic content rather than
language-specific formatting differences (Appendix~\ref{app:human}).
Agreement metrics are computed over 339 question--variant pairs
per human and per model.

\subsection{Metrics and Evaluation Protocols}
\label{sec:eval_protocols}

\textbf{Task Performance.} Although no image is provided at inference time,
we report blind VQA task accuracy as a complementary behavioral signal of how
models respond to question-only inputs. We include it as a secondary metric
because it also indicates how strongly models leverage question-side priors,
biases, and shortcuts rather than true visual grounding under blind answering
\citep{chen2024are,lee2024vlind}.

\textbf{Alignment.} For semantic agreement, we compute pairwise
Sentence-BERT cosine similarity \citep{wang2020minilm} between responses,
applied to human--human (HH) pairs (measuring within-group consensus),
human--model (HM) pairs (measuring alignment to human priors), and
model--model pairs. This allows us to compare how closely models approximate
the human prior structure at the per-question level. Because embedding-based
agreement can be mildly affected by response length before cosine comparison,
we use raw SBERT as the primary semantic metric and report truncated-answer
robustness tables in Appendix~\ref{app:metrics}; these are mainly relevant
for more verbose standalone LLM outputs. We additionally report lexical
overlap metrics (chrF, ROUGE-1, exact match) as robustness checks in
Appendix~\ref{app:metrics}.
% \textbf{Difficulty correlation} is Pearson~$r$ between per-question human
% accuracy and per-model accuracy. \textbf{Krippendorff's $\alpha$} (nominal)
% measures within- and cross-group reliability. \textbf{Token confidence} is
% mean token probability from output logprobs.

\textbf{Preprocessing.} All answers undergo VQA-standard preprocessing before scoring: stripping
articles (\textit{a, an, the}), normalizing punctuation, and converting
and common count words (e.g., \textit{three}, \textit{twelve}) to digits for
both human and model responses \citep{antol2015vqa}. Generic answer wrappers
(e.g.,\ \textit{``answer is ...''}) are removed before normalization, and
Korean answers are translated to English and then processed with the same
VQA-style normalization used elsewhere in the paper. These translated and
normalized strings are the inputs to the pairwise human--human and
human--model agreement calculations.

\textbf{Shared substantive-response evaluation} addresses the confound that
some models abstain while others answer. For this robustness view, we remove
hard and soft abstentions, strip generic answer wrappers (e.g.,
\textit{``The answer is''}), apply VQA answer normalization, and recompute
agreement on the remaining substantive responses only. This isolates
content-level agreement from refusal behavior while preserving the paper's
main raw-answer evaluation in the primary results.

\textbf{Length-normalized evaluation} additionally truncates all answers to
$\leq$5 words after preprocessing, controlling for the format mismatch
between terse VLM outputs and verbose standalone LLM
responses. This primarily affects reasoning-mode (think) models
($\Delta\text{SBERT}{=}{+}0.046$) and has negligible impact on VLMs and
backbone decoders ($\Delta\text{SBERT}{<}{+}0.006$); standalone LLMs
without think mode are also minimally affected
($\Delta\text{SBERT}{<}{+}0.02$). We report both raw and
length-normalized tables in the supplement
(Tables~\ref{tab:comprehensive_inst_blind}
and~\ref{tab:comprehensive_inst_blind_w5}) to confirm that group rankings
are preserved regardless of truncation.

% ─────────────────────────────────────────────────────────────────────────────
% ═══════════════════════════════════════════════════════════════════════════
% §4  HUMAN-MODEL BLIND ALIGNMENT (LEAD SECTION)
% ═══════════════════════════════════════════════════════════════════════════
\section{Human-Model Blind Alignment}
\label{sec:alignment}

We begin with a basic comparison: when models answer VQA questions
without seeing any image, how closely do their ``imagined'' answers
match those of humans under the same condition? Rather than measuring
accuracy against ground truth---which conflates linguistic prior
exploitation with genuine visual reasoning---we measure \emph{semantic
agreement} between blind model outputs and blind human responses. The
results section proceeds from overall alignment to category-level patterns,
then to image-condition ablations, and finally to robustness analyses.

\subsection{Human Reference Patterns}
\label{sec:human_patterns}

To interpret model-human agreement, we first need the structure of human
blind agreement itself. We therefore measure pairwise human--human (HH)
SBERT per question across all three entity variants
(original $\to$ weaker-object $\to$ pronominalized). All answers---human
and model alike---are passed through the same normalization pipeline
(wrapper stripping and VQA normalization; \S\ref{sec:eval_protocols})
before computing SBERT similarity. Throughout, we use mean pairwise HH
agreement as a human reference ceiling: because humans themselves disagree,
this quantity is below 1 and should be interpreted as a behavioral reference
level rather than a hard upper bound.

Figure~\ref{fig:human_degradation} shows the distribution of
per-question HH SBERT by operation type and entity group across
variants. Operation type is the primary axis along which human
agreement varies: a one-way ANOVA of HH SBERT by operation type is
significant in every variant ($F{=}5.2$, $6.6$, $8.3$ for the
original, weaker-object, and pronominalized variants, respectively;
all $p{<}10^{-5}$). This operation-level separation remains robust
under progressively weaker lexical formulations and is descriptively
stronger under abstraction (Kruskal-Wallis $H{=}33.6 \to 41.6$,
original$\to$pronominalized). The associated effect sizes are practically
meaningful rather than merely significant, and the same conclusion holds under
robust variance checks (Appendix~\ref{app:chrf_human}). Entity group, by contrast, is never significant
($p{>}0.38$ in every variant).

However, the degradation under entity removal is not uniform. Paired $t$-tests
within each operation type (original vs.\ pronominalized) reveal that identity questions
collapse ($0.534 \to 0.337$, $p{=}0.002$), whereas count questions
remain stable ($0.662 \to 0.671$, $p{=}0.55$). Among entity groups,
food shows one of the larger observed drops ($0.566 \to 0.470$, $p{=}0.017$),
with animal and object groups showing borderline drops ($p{\approx}0.06$);
person and other groups are stable ($p{>}0.28$). The highest-HH questions
in the diagnostic ranking make this concrete: \emph{``How many bottles of
water are on the table?''} (count/object, HH${=}0.783$),
\emph{``Do the horses love each other?''} (action/animal, HH${=}0.770$),
and \emph{``How many hot dogs are on this bun?''} (count/food,
HH${=}0.770$) all lie in operations with tightly constrained answer spaces.
By contrast, the lowest-HH questions cluster in entity- and visually grounded
categories: \emph{``What kind of information does the yellow and white book
look like it contains?''} (identity/other, HH${=}0.183$),
\emph{``What does the shirt say?''} (text/object, HH${=}0.219$), and
\emph{``What does the man's gesture mean?''} (attribute/person,
HH${=}0.226$). These examples match Figure~\ref{fig:human_degradation}:
high-consensus human priors are concentrated in count and some action
questions, while low-consensus cases are disproportionately identity,
text, and person/other-grounded questions that require missing visual detail.
Identity is the clearest operation-level example of this anchor sensitivity in
our human subset. When the noun is preserved, questions such as \emph{``What
animal is this cake meant to represent?''}, \emph{``What type of bear is
this?''}, and \emph{``What type of shoe is that?''} still elicit partial
consensus around a compact set of plausible answers; once the noun is
pronominalized, the same questions spread across multiple candidate referents
and HH agreement drops sharply (Table~\ref{tab:identity_focus_examples}).
This complements the stable count examples above: count often remains compact
because its answer space is already narrow, whereas identity opens into a
broader set of plausible guesses once the lexical anchor is removed.

Human abstention is negligible: only 0.25\% of responses are explicit
abstentions (e.g.,\ ``unknown'', ``I don't know''), rising marginally
from 0.22\% (Original) to 0.29\% (Pronominalized). Unlike models---where
the cleaning pipeline strips refusals and generic wrappers---human answers
pass through essentially unchanged (0.7\% of HH
answer tokens are affected, all by minor VQA normalization). The few
``unknown'' responses that remain are concentrated on questions that
genuinely require visual content (e.g.,\ brand identification,
ethnicity), confirming that they reflect true ambiguity rather than
participant disengagement. For human--model (HM) comparison, the shared
pipeline ensures that SBERT scores reflect semantic content rather than
format mismatch: model refusals and preambles are stripped before
embedding, so low human--model SBERT indicates genuine disagreement rather than
verbosity artifacts.

These patterns are robust to entity manipulation: per-question HH SBERT
correlates $r{>}0.77$ across all variant pairs, and overall human agreement
decreases only modestly under pronominalization. This gives the reference
point for the model analysis below: humans do lose some agreement when
entity cues are stripped, but the loss is concentrated in a few operations
rather than being global.
Within the human reference itself, identity shows the clearest observed
degradation, while count provides the most stable contrast case.

\begin{figure*}[t]
\centering
\includegraphics[width=0.49\textwidth]{human_hh_sbert_by_op_variant}\hfill
\includegraphics[width=0.49\textwidth]{human_hh_sbert_by_entity_variant}
\caption{Human--human SBERT degradation across control variants.
\textbf{Left:} by operation---identity collapses most ($\Delta{=}{-}0.198$); count remains stable.
\textbf{Right:} by entity---weaker separation than operations, confirming human blind priors are structured primarily by operation type.}
\label{fig:human_degradation}
\end{figure*}

\begin{figure}[t]
\centering
\includegraphics[width=0.45\textwidth]{inst_blind_qwen3_vl_8b_yesno_abstsplit}\\[0.5em]
\includegraphics[width=0.45\textwidth]{inst_blind_qwen3_vl_8b_number_abstsplit}
\caption{Blind answer distributions for the Qwen3 family (top: yes/no,
bottom: count). Ground truth uses all 10 per-question VQA annotator
responses. All-model version in Figure~\ref{fig:dist}.}
\label{fig:dist_main}
\end{figure}

\subsection{Control-Variant Trends}
\label{sec:degradation}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{abstention_filtered/inst_blind_accuracy_groups_3step_q113_h40_yesno_filtered}\\[-0.25em]
\includegraphics[width=\textwidth]{abstention_filtered/inst_blind_sbert_groups_3step_q113_h40_yesno_filtered}
\caption{Per-model degradation across control variants (7--8B scale).
Top: blind accuracy; bottom: human--model SBERT. Gray ribbon: human reference $\pm$95\% CI.
Solid lines: raw evaluation; dotted: abstention-filtered.
Backbone decoders stay closest to the human trajectory; standalone LLMs degrade most.}
\label{fig:variants}
\end{figure*}

Figure~\ref{fig:variants} summarizes per-model degradation as content noun
phrases are weakened from the original question to a hypernym-weakened
form and then to a fully pronominalized form. Two regularities are most
visible:

\textbf{The backbone decoder paradox.} Across all three variants,
backbone decoders achieve the highest human-model SBERT
agreement---consistently above full VLMs at equivalent scale and approaching
the human-human ceiling ($\overline{\text{SBERT}}_{\text{HH}} = 0.594$).
Removing the vision encoder does not hurt human alignment;
it \emph{improves} it. Vision co-training embeds human-like priors in the
language backbone, but the vision encoder active on a blank image introduces
distributional biases that push answers away from the human prior.

\textbf{Lexical-anchor dependence.} Model accuracy drops 10--13 pp from
the original to the pronominalized formulation while human accuracy barely
changes. The SBERT lineplot shows
the same degradation semantically: standalone LLM groups collapse most
steeply, while backbone decoders maintain the highest alignment throughout.
The human-human ceiling drops only slightly, confirming that human blind
reasoning does not depend on content-noun anchors in the same way as models.
The same backbone $>$ VLM relation also holds within individual model
families, with the widest paired gap in InternVL and a notable low-alignment
outlier at Qwen3-VL-32B (Appendix~\ref{app:family_alignment}).
Among the tested groups, backbone decoders remain the closest to the human
trajectory under lexical weakening, with InternVL providing the clearest
within-family backbone-over-VLM contrast.

\subsection{Scale Trends}
\label{sec:scale}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{scale_metrics/inst_blind_accuracy_groups_vC_q113_h40_yesno}

\vspace{0.4em}
\includegraphics[width=\columnwidth]{scale_metrics/inst_blind_agreement_groups_sbert_vC_q113_h40_yesno}
\caption{Blind accuracy (top) and human--model SBERT (bottom) vs.\ parameter count.
Backbone decoders approach the human--human SBERT ceiling while VLMs exceed
the human accuracy baseline---accuracy and alignment track different aspects
of blind answering.}
\label{fig:scale}
\end{figure}

Figure~\ref{fig:scale} separates two related effects: how much of the
blind-answer pattern comes from model group, and how much from scale? On
blind accuracy, VLMs and backbone decoders both exceed the human baseline
across parameter counts, while standalone LLMs fall far below. Complementary
agreement analyses show that backbone decoders approach the human-human
ceiling at 7--8B, while full VLMs at equivalent scale remain lower. This
model-group gap persists across sizes, but scale still matters within each
family: the largest VLMs (32B) do not converge with their backbones, and the
scale $\times$ question-variant analysis (Appendix~\ref{app:scale_variant})
shows that larger standalone LLMs can become \emph{more}
anchor-sensitive rather than less. We therefore treat model group and scale
as distinct viewpoints on the same phenomenon: model group sets the broad
blind-prior regime, while scale and family-specific nuisance behavior change
how strongly that regime is expressed. We treat substantive-response
filtering as a supplementary robustness check rather than a separate
evaluation regime; it narrows some absolute gaps for verbose
models but does not change the group ordering
(Appendix~\ref{app:metrics}; Table~\ref{tab:comprehensive_inst_blind}). At
this scale, the strongest blind-accuracy models are full VLMs, whereas
backbone decoders remain closer to the human reference on semantic alignment.

\subsection{Aggregate Agreement Levels}
\label{sec:model_similarity}

Answer length varies substantially across model groups: VLMs and backbone
decoders produce median 1-word answers, while standalone LLMs average 6.9
words (Mistral-7B: 29.7 words). Since embedding-based metrics penalize
length mismatch, we use raw SBERT as the primary semantic metric and treat
length-normalized and lexical alternatives as supplementary robustness checks
(Appendix~\ref{app:metrics}).

We compute pairwise SBERT cosine across questions within the human subset
for all models. The scale-dependent analyses
(Figure~\ref{fig:scale}) exclude Mistral-7B and Phi-3.5-mini due to
degenerate blind outputs (verbose refusals and near-zero accuracy,
respectively; see Appendix~\ref{app:scale_variant}).
The human-human ceiling is
$\overline{\text{SBERT}}_{\text{HH}} = 0.594$.
Backbone decoders reach $0.586$---closest to the ceiling---while full
VLMs score $0.557$ and standalone LLMs $0.476$.

High SBERT proximity to humans co-occurs with a large accuracy \emph{advantage}
over humans: VLMs outperform humans by $+20$\% on average, backbone decoders
by $+16$\%. In the blind setting, this should \emph{not} be interpreted as
stronger reasoning, since the image is absent and success can only come from
prior exploitation. Higher blind accuracy therefore indicates stronger
convergence on benchmark-favored shortcut answers, not more faithful visual
grounding. The key distinction is whether a model's blind prior resembles the
human prior or collapses onto a narrower, non-human bias. In this sense,
backbone decoders are more human-aligned than full VLMs even when full VLMs
achieve high blind accuracy. This interpretation is consistent with
\citet{bao2026imagination}: the issue is not that models rely on priors at
all, but that they converge too aggressively on a narrow bias that helps on
blind benchmark labels while being unhelpful for pure visual grounding.
Standalone LLMs show both lower SBERT (0.48) and a \emph{negative} accuracy
gap ($-8$\%), confirming they fail to exploit the same shortcut distribution.
Under substantive-response filtering, the standalone LLM gap narrows but the
ranking is preserved, confirming that prior-content differences---not just
refusal behavior or format mismatch---drive the separation. As the
distribution analysis later shows, part of this gap comes from standalone LLMs
placing much more mass on heterogeneous ``other'' answers rather than on the
compact VQA-style answer sets that humans and decoders more often occupy.
This contrast is visible even at the model level. On the instruction-only
original blind condition, the highest blind-accuracy models are
LLaVA-Mistral, LLaVA-1.5 (LM), and LLaVA-Mistral (LM), all at 0.537,
whereas Mistral-7B falls to 0.000. Yet the strongest human--model SBERT in
the same condition comes from InternVL-8B (0.619), with several backbone
decoders clustered close behind. High blind accuracy therefore does not by
itself signal stronger human alignment; among the tested model groups,
backbone decoders remain closest to the human reference overall.

% ═══════════════════════════════════════════════════════════════════════════
% §5  CATEGORY-LEVEL PATTERNS
% ═══════════════════════════════════════════════════════════════════════════
\section{Category-Level Patterns}
\label{sec:drivers}

\subsection{Question-Level Alignment Structure}
\label{sec:prior_types}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{question_diagnostic/hh_vs_hm_scatter_7b}
\caption{Per-question human--human (HH) vs.\ human--model (HM) SBERT for the matched 7--8B model set.
Each panel shows one model across all three variants; proximity to the diagonal
indicates that the model tracks the same question-level agreement structure as
humans. Backbone decoders are generally tighter to the diagonal than full VLMs
or standalone LLMs.}
\label{fig:hh_hm_scatter}
\end{figure*}

Figure~\ref{fig:hh_hm_scatter} shows that the backbone--decoder advantage is
not only an average-group effect. The strongest backbone decoders remain
tighter to the human diagonal across original, weaker, and pronominalized
questions, indicating that they preserve the same per-question alignment
structure that humans do. The supplementary correlation table
(Table~\ref{tab:hh_hm_scatter_corr}) makes this explicit: decoder panels such
as LLaVA-Mistral (LM) and LLaVA-1.5 (LM) reach $r{\approx}0.93$, whereas the
best full VLMs are slightly lower and standalone LLMs are markedly weaker.

This question-level view complements the aggregate variant ladder. A model can
have reasonable mean HM agreement while still missing the human easy/hard
structure across questions; conversely, a model can follow that structure but
be shifted downward overall. We therefore treat HH$\to$HM correlation as a
structural complement to the raw alignment level, not a replacement for it.
Grouped operation/entity summaries remain useful for interpretation and are
reported in the supplementary material (Figure~\ref{fig:op_hm};
Tables~\ref{tab:group_corr_op} and~\ref{tab:group_corr_ent}). At that grouped level,
InternVL-8B provides the closest operation-level match to the human profile
($\rho=0.982$), while LLaVA-Mistral (LM) is strongest on entity groups
($\rho=0.900$).
The dense upper-right region of Figure~\ref{fig:hh_hm_scatter} is populated
mainly by constrained-answer questions, especially yes/no and some count
items, where both human consensus and human--model agreement are relatively
high. This concentration reflects both genuinely stronger shared priors and
the fact that compact answer spaces are easier to align under SBERT, so the
scatter is interpreted jointly with the answer-distribution analysis below.

The same answer-distribution mechanisms remain visible beneath this scatter.
Identity questions are especially sensitive to anchor removal in the human
data, whereas count questions often retain high human consensus but induce
non-human model defaults. Figure~\ref{fig:dist_main} makes this concrete for
the Qwen3 family: the backbone decoder tracks the human yes/no split
(58\% yes vs.\ 65\% human) while the full VLM inverts it (62\% no), and on
count questions the VLM collapses to a single bin (80\% ``2--3'') while the
backbone distributes more broadly. More generally, standalone LLMs assign a
larger share of probability mass to heterogeneous ``other'' answers on these
question types, indicating that they often fail to enter the short VQA-style
answer manifold at all. This is plausibly related to training mismatch:
unlike VLMs and their paired decoders, text-only LLMs are not optimized on the
same VQA answer format, so their blind answers more often drift into generic
descriptions, hedges, or off-manifold guesses (e.g., yes/no-type outputs such
as \emph{``asleep''}, \emph{``closed''}, or \emph{``not always''}, and
count-type outputs such as \emph{``many''}, \emph{``half''}, or
\emph{``infinite''}). That broader ``other'' region is one reason their
human--model alignment remains lower even when abstentions are filtered.
At the per-question level, backbone decoders most consistently track the
human agreement structure, and InternVL-8B is the single
highest-correlation panel in the matched set.

% ═══════════════════════════════════════════════════════════════════════════
% §6  IMAGE-CONDITION ABLATION
% ═══════════════════════════════════════════════════════════════════════════
\section{Image-Condition Ablation}
\label{sec:ablation}

The backbone decoder comparison raises a related question: is the blank image
simply an adversarial input that misleads the vision encoder, or does the
encoder fundamentally lack a neutral mode? If the former, a different
uninformative image might recover alignment; if the latter, backbone
decoders should outperform VLMs under \emph{any} uninformative visual input.

\begin{table}[t]
\centering
\caption{Image-condition ablation (original formulation).
No uninformative image recovers backbone-level alignment.}
\label{tab:image_ablation_main}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{llcccc}
\toprule
\textbf{Model} & \textbf{Image} & \textbf{Acc.} & \textbf{chrF} & \textbf{SBERT} & \textbf{Soft-abs.} \\
\midrule
\multirow{4}{*}{Qwen3-VL-8B} & Blank & 0.478 & 0.215 & 0.541 & 8.0\% \\
 & Gray  & 0.357 & 0.172 & 0.473 & 21.2\% \\
 & Noise & 0.277 & 0.136 & 0.399 & 36.3\% \\
 & White & 0.466 & 0.223 & 0.549 & 5.3\% \\
\midrule
\multirow{4}{*}{LLaVA-Mistral} & Blank & 0.558 & 0.197 & 0.534 & 8.8\% \\
 & Gray  & 0.543 & 0.195 & 0.535 & 8.0\% \\
 & Noise & 0.478 & 0.180 & 0.503 & 11.5\% \\
 & White & 0.519 & 0.198 & 0.531 & 8.8\% \\
\bottomrule
\end{tabular}
\end{table}

We test this with uninformative image conditions---blank, uniform gray, random
noise, and uniform white---on representative VLMs from two families
(Table~\ref{tab:image_ablation_main}). The main result is that once the vision
pathway is active, uninformative images perturb blind priors rather than
recovering a neutral no-vision regime. For Qwen3-VL-8B, moving from blank to
gray to noise lowers both blind accuracy and human alignment: accuracy falls
from 0.478 to 0.277, human--model SBERT from 0.541 to 0.399, and human--model
chrF from 0.215 to 0.136. LLaVA-Mistral is more stable overall, but still drops
under noise, showing that the effect is not unique to one family. The backbone
decoder is not shown as another table row because it has no image pathway at
all; it serves as the no-vision reference. Under every uninformative image
condition, the full VLM remains below that decoder-level alignment, and moving
from blank to gray or noise increases answer instability rather than restoring
the backbone behavior. Even when white slightly outperforms blank for
Qwen3-VL-8B on human--model metrics, the overall pattern remains image-condition
sensitive rather than converging to a decoder-like blind mode. We return to
related within-family heterogeneity in the later scale analysis.

Taken together, these results reframe the backbone decoder paradox:
vision co-training embeds useful visual-linguistic associations in the
language backbone, but the vision encoder itself has no mechanism to signal
\emph{``I have nothing useful to contribute.''} It always contributes,
and on uninformative input that contribution is noise that pushes the
language model away from the human-like linguistic prior it would otherwise
express. This explains why the backbone decoder---free from this
modulation---produces the most human-aligned blind responses.
Among the tested full VLMs, LLaVA-Mistral is the most stable across
uninformative image conditions, whereas Qwen3-VL-8B is the most
image-sensitive; neither family recovers the decoder-level reference.

% ═══════════════════════════════════════════════════════════════════════════
% §7  INSTRUCTION EFFECTS AND CONFUND CONTROLS
% ═══════════════════════════════════════════════════════════════════════════
\section{Confound Controls}
\label{sec:confounds}

We verify that the agreement patterns above are not measurement artifacts
of abstention or answer format. Across all checks, the central ordering
(backbone $>$ VLM $>$ standalone LLM) is preserved.

Adding the blind-awareness instruction increases human--model agreement modestly
(${\approx}{+}0.02$ SBERT), with the largest gains on the original
variant and smaller gains as entity cues are removed
(Appendix~\ref{app:inst}). Instruction framing nudges models toward
human-like blind answers but does not overturn the architectural ordering
or remove the backbone advantage.

\subsection{Abstention Effects}

Hard abstentions (explicit refusals) range from 0--18\% across VLMs
and 0--5\% across backbone decoders, with outliers at Qwen3-VL-32B
(17.7\%). Soft abstention---implicit
hedges such as ``unknown'' or ``nothing''---is 0--11\% for VLMs and
0--3\% for backbones; standalone LLMs show 0--14\% soft and up to
30\% hard (Mistral-7B)
(Appendix~\ref{app:abstention_calibration}).
The shared substantive-response comparison restricts to questions on which
models produced non-abstaining answers. human--model SBERT increases modestly under
filtering, but the backbone--VLM--standalone ordering is preserved,
showing that the main pattern is not an artifact of abstention-heavy items.
After these controls, the backbone-decoder group still remains closest to
human answers, indicating that the main alignment result is not a refusal or
formatting artifact.

% ─────────────────────────────────────────────────────────────────────────────
\section{Discussion}

\paragraph{The vision encoder has no neutral mode.}
Our central finding is that removing the vision encoder from a VLM
\emph{improves} blind human-model alignment. Image-condition ablations
confirm this is not a blank-image artifact: the vision encoder actively
modulates outputs on any uninformative image, and no condition recovers
backbone-level alignment. Vision co-training embeds human-like
linguistic priors in the language backbone, but the encoder itself
introduces distributional biases that push answers away from those priors
when no meaningful visual content is present. This has practical
implications: blind VQA diagnosis should test backbone decoders
separately, and VLM architectures may benefit from a learned
``visual uncertainty'' signal or explicit gating mechanism that suppresses
encoder influence when input is uninformative.

\paragraph{An information-theoretic view.}
The backbone decoder computes $P(a \mid q)$ from the question alone---a
distribution shaped by the co-occurrence statistics of VQA fine-tuning
data, which closely mirrors human linguistic priors. The VLM computes
$P(a \mid q, v_0)$, where $v_0$ is the vision encoder's output for an
uninformative image. Critically, $v_0 \neq \mathbf{0}$: the encoder maps
any input---blank, gray, noise---to a non-trivial embedding that
carries its own distributional biases. This embedding acts as a
nuisance variable that shifts the output distribution away from
$P(a \mid q)$ without adding information.
Per-question entropy analysis (Appendix~\ref{app:case_study}) provides
three confirmations. First, human--model alignment correlates strongly
with shared uncertainty ($r = -0.82$ between human answer entropy and
human--model SBERT): questions where humans agree are exactly the questions where
models align with humans. Second, models show systematically higher
consensus than humans---their answer entropy falls below the human
diagonal on the majority of questions, with the gap largest on
entity-grounded categories (identity, attribute) where the encoder's
``default image'' representation activates spurious visual associations.
Third, entropy mismatch is orthogonal to lexical-anchor dependence
($r = 0.12$, n.s.), ruling out answer consensus as a confound for the
control-variant findings: the mechanism by which $v_0$ distorts
$P(a \mid q)$ is separate from the mechanism by which content nouns
activate specific priors. In this sense, the entropy mismatch is not just a
descriptive metric: it indicates a qualitative divergence in uncertainty
behavior. Humans manage underspecification through heuristics that preserve
plural plausible answers, whereas models often collapse that uncertainty into
a narrower shortcut answer.

\paragraph{Lexical anchors as prior activators.}
The control-variant degradation chain reveals that blind model success is
predominantly carried by content-noun lexical cues. When these are
stripped, models diverge from human priors while humans barely change.
This identifies a concrete mechanism---lexically anchored prior recall---that
benchmark design and debiasing interventions can explicitly target.
We note that benchmark contamination may amplify this effect:
\citet{chen2024are} show that VLMs can achieve non-trivial scores on
multimodal benchmarks without visual input, partly due to training-set
leakage. Our control-variant design partially disentangles the two
sources---pronominalized questions remove the lexical cues that
memorization would exploit, yet models still retain above-chance
performance, suggesting that corpus-level priors contribute beyond
direct memorization. A stronger mechanistic test would relate shortcut answer
frequencies directly to pretraining or fine-tuning corpus statistics; we view
that correlation analysis as a natural next step for strengthening the prior
account.

\paragraph{Implications for benchmark design.}
Any VQA benchmark can be partially solved by linguistic priors, and the
magnitude varies predictably with question type and lexical anchoring.
We propose that blind VQA evaluation become a standard diagnostic: blind
accuracy above 40\% signals over-indexing on knowledge-accessible
question types.

\paragraph{Limitations.}
Human participants were recruited from university communities in
Korean/English contexts, limiting demographic generalizability. Model
evaluation uses greedy decoding (temperature~0), which may underestimate
answer diversity. Human data was collected under the explicit blind-awareness instruction only.
Image-condition ablations cover five VLMs from all three families
but use only four uninformative image types; a broader stimulus set
(e.g., task-irrelevant natural images) could further characterize encoder behavior.
A direct corpus-frequency correlation would strengthen the mechanistic
account \citep{mccoy2024embers}; specifically, future work should test whether
shortcut answer frequencies track corpus-level answer statistics for the same
question forms.

% ─────────────────────────────────────────────────────────────────────────────
\section{Conclusion}

We present a human-calibrated diagnostic study of linguistic prior
exploitation in Vision-Language Models. The central finding is
architectural: backbone decoders---the same VLM weights with the vision
encoder bypassed---achieve near-human semantic agreement (HM SBERT 0.586,
compared with a human--human reference level of 0.594) while full VLMs
score lower (0.557) and standalone LLMs diverge further. Image-condition
ablations show this is not a
blank-image artifact: the vision encoder has no neutral mode and actively
distorts the backbone's linguistic prior on any uninformative input.

Control question variants show that blind model success is carried by
content-noun lexical anchors: as these are stripped, model--human
agreement drops monotonically while the human ceiling barely moves. The
resulting pronominalization test offers a simple diagnostic for separating
lexically anchored shortcut behavior from more robust visual grounding. The
pattern is stable across scales and robust to confound controls
(abstention filtering, length normalization, instruction effects).
Question-type
decomposition identifies where alignment holds (yes/no, action) and
where it breaks (count, grounded reference), providing a diagnostic
vocabulary for linguistic prior exploitation in any multimodal benchmark.

% ─────────────────────────────────────────────────────────────────────────────
\bibliography{paper}

% ─────────────────────────────────────────────────────────────────────────────
\appendix
\clearpage
\setcounter{figure}{0}
\setcounter{table}{0}
\renewcommand{\thefigure}{A\arabic{figure}}
\renewcommand{\thetable}{A\arabic{table}}

\section{Human Study Protocol}
\label{app:human}

A total of 40 participants were included. Participants were recruited via
online student community forums at universities. All procedures were conducted
under an IRB-approved protocol adhering to the Declaration of Helsinki.
Informed consent was obtained prior to participation.

Participants answered text-only explicit no-image blind-instruction questions
across the original, weaker-object, and pronominalized formulations. The
analysis applies a raw-response qualification threshold
($\geq$348) before subset intersection, then restricts to the question IDs
answered by all qualifying participants across all three reformulations
(339 question--variant pairs per participant).
Participants were compensated at 10,000~KRW per hour. Responses in Korean or
English were translated and processed using the aggregation strategy in
Appendix~\ref{app:aggregation}.

As a supplementary human-only diagnostic, we also examine the relationship
between per-question answer entropy and human VQA accuracy. Across variants,
the two are strongly negatively correlated ($r\approx -0.77$), indicating that
questions eliciting more diverse human blind answers are also the questions on
which humans are less likely to match VQA ground truth. We treat this as a
difficulty/ambiguity check rather than a primary result, since the main paper
focuses on semantic agreement and anchor sensitivity rather than accuracy.

\section{Answer Aggregation}
\label{app:aggregation}

We cluster semantically equivalent answers (e.g., ``3'' and ``three'') using
GPT-4o mini \citep{openai2025gpt4o}. Human responses are aggregated into a
confidence-weighted distribution:
\begin{equation}
H(a_k^*) = \frac{\sum_{i:\phi(a_i)=a_k^*} c_i}{\sum_i c_i}
\end{equation}
where $c_i$ is participant $i$'s Likert confidence (normalized to $[0,1]$)
and $\phi$ maps raw answers to canonical forms.

\section{Control Question Extraction Prompt}
\label{app:ctl}

Control variants are extracted using GPT-4.1-mini-2025-04-14 (temperature~0,
seed~42, JSON schema mode) in batches of 50 questions per API call.
The output schema enforces
\texttt{\{deictic\_removed, object\_removed, weaker\_object, pronominalized\}}
as required string fields with \texttt{additionalProperties:~false}.
The system prompt is reproduced verbatim below:

\begin{quote}\small
Generate 4 control variants for each VQA question.
Return ONLY a JSON object \texttt{\{"results":[...]\}}; preserve input order.

\textbf{Rules:}

1) \textit{deictic\_removed}: Remove deictic words (this/that/these/those/here/there)
and spatial markers (left/right/top/bottom/foreground/background) while preserving
grammar. Restructure the sentence if needed to remain fluent.

2) \textit{object\_removed}: Replace the specific object noun with neutral placeholder
``object''. Do not replace pronouns or generic nouns already present.

3) \textit{weaker\_object}: Replace the specific object noun with a broader hypernym
one level up (dog$\to$animal, car$\to$vehicle, drink$\to$beverage, shirt$\to$clothing,
apple$\to$fruit, knife$\to$utensil, sofa$\to$furniture). If no clear hypernym
exists, use ``object''.

4) \textit{pronominalized}: Replace ALL content noun phrases with minimal pronouns.
Grammar-aware rules: attribute (``What color is the dog?'') $\to$ ``What color is it?'';
count (``How many cats are there?'') $\to$ ``How many are there?'';
location (``Where is the phone?'') $\to$ ``Where is it?'';
action (``What is the man doing?'') $\to$ ``What is it doing?'';
yes/no (``Is the dog affectionate?'') $\to$ ``Is it affectionate?'';
multi-NP (``between the girl and the giraffe'') $\to$ ``between them''.
Keep ONLY question words, copula, prepositions, predicate/attribute, and pronouns.
Use ``it''/``they''/``them''---never ``the object''.
\end{quote}

\section{Question Embedding Coverage}
\label{app:tsne}

Figure~\ref{fig:tsne} shows t-SNE projections of the 1,000-question pool
(all-mpnet-base-v2) with the human-study question subset highlighted.
The subset covers the full semantic range of the pool across both entity
and operation dimensions, confirming that human-comparison results are not
restricted to a narrow semantic region.

\begin{figure*}[t]
\centering
\includegraphics[width=\columnwidth]{subset_tsne_entities}%
% \hspace{0.01\columnwidth}%
\includegraphics[width=\columnwidth]{subset_tsne_operations}
\caption{t-SNE of the 1k VQA pool (gray) with the human-study
subset highlighted. \textbf{Left:} colored by entity type.
\textbf{Right:} colored by operation type. The subset is distributed
across the full embedding space.}
\label{fig:tsne}
\end{figure*}

\section{Comprehensive Multi-Metric Blind-With-Instruction Performance}
\label{app:metrics}

Tables~\ref{tab:comprehensive_inst_blind}
and~\ref{tab:comprehensive_inst_blind_w5} report agreement across all metrics.
chrF scores are computed with the \texttt{sacrebleu} implementation.

\begin{table*}[t]
\scriptsize
\centering
\setlength{\tabcolsep}{2.5pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lc|cccccc|cccccc|cccccc}
\toprule
\textbf{Model} & \textbf{Size} & \multicolumn{6}{c|}{\textbf{Original}}& \multicolumn{6}{c|}{\textbf{Weaker}}& \multicolumn{6}{c}{\textbf{Pronominalized}} \\
\cmidrule(lr){3-8}\cmidrule(lr){9-14}\cmidrule(lr){15-20}
 & & \textbf{Acc} & \textbf{Len} & \textbf{SBERT} & \textbf{chrF} & \textbf{ROUGE-1} & \textbf{Exact}& \textbf{Acc} & \textbf{Len} & \textbf{SBERT} & \textbf{chrF} & \textbf{ROUGE-1} & \textbf{Exact}& \textbf{Acc} & \textbf{Len} & \textbf{SBERT} & \textbf{chrF} & \textbf{ROUGE-1} & \textbf{Exact} \\
\midrule
\textbf{Human (N=40)} & -- & 26.3 & 1.3 & 59.4 & 27.2 & 23.9 & 22.6 & 22.9 & 1.3 & 57.8 & 26.7 & 23.3 & 22.4 & 22.6 & 1.3 & 55.9 & 24.1 & 20.7 & 20.2 \\
\midrule
\multicolumn{20}{c}{\textit{Vision-Language Models}} \\
\midrule
\multirow{3}{*}{InternVL} & 1B & 36.9 & 1.2 & \underline{57.7} & 23.2 & 19.3 & 18.5 & 36.6 & 1.2 & 54.8 & 21.6 & 18.3 & 17.9 & 33.6 & 1.2 & \underline{53.4} & \underline{21.6} & \underline{18.3} & \underline{18.0} \\
 & 2B & 44.5 & 1.3 & 55.4 & 20.9 & 17.1 & 15.7 & 38.6 & 1.2 & \underline{55.6} & 21.8 & 18.4 & 17.6 & 39.2 & 1.3 & 52.7 & 20.3 & 16.5 & 16.2 \\
 & 8B & 37.2 & 1.5 & \textbf{61.2} & \textbf{27.4} & \textbf{22.9} & \textbf{21.5} & 33.3 & \underline{1.7} & \textbf{59.0} & \textbf{26.2} & \textbf{22.9} & \textbf{22.1} & 29.5 & \underline{1.7} & \textbf{55.5} & \textbf{22.4} & \textbf{19.1} & \textbf{18.3} \\
LLaVA-1.5 & 7B & 47.2 & 1.0 & 51.9 & 18.6 & 15.0 & 15.0 & \textbf{47.5} & 1.0 & 50.8 & 16.8 & 13.9 & 13.7 & 35.4 & 1.1 & 48.5 & 16.2 & 13.5 & 13.6 \\
LLaVA-1.6-Mistral & 7B & \textbf{53.7} & 1.1 & 55.6 & 21.8 & 17.8 & 17.2 & \underline{46.0} & 1.1 & 53.9 & 20.6 & 17.5 & 17.3 & \underline{41.3} & 1.1 & 50.2 & 17.9 & 14.6 & 14.5 \\
\multirow{2}{*}{LLaVA-1.6-Vicuna} & 7B & 51.6 & 1.0 & 54.4 & 20.8 & 17.4 & 16.7 & 44.2 & 1.0 & 52.2 & 18.9 & 15.6 & 15.3 & \textbf{42.3} & 1.0 & 50.9 & 18.2 & 15.0 & 14.9 \\
 & 13B & \underline{52.4} & 1.0 & 56.3 & \underline{23.8} & \underline{20.4} & \underline{20.1} & 38.6 & 1.1 & 54.5 & 22.8 & 19.7 & 19.4 & 37.5 & 1.0 & 51.9 & 18.5 & 15.3 & 15.3 \\
\multirow{4}{*}{Qwen3-VL} & 2B & 47.6 & 1.3 & 56.1 & 21.6 & 17.5 & 16.3 & 41.9 & 1.3 & 53.4 & 20.6 & 16.6 & 16.4 & 40.2 & 1.3 & 52.6 & 19.0 & 15.6 & 15.4 \\
 & 4B & 50.1 & 1.2 & 56.8 & 23.5 & 19.2 & 18.3 & 37.5 & 1.2 & 55.2 & 23.9 & 20.2 & 19.9 & 38.7 & 1.1 & 52.7 & 20.7 & 17.5 & 17.1 \\
 & 8B & 47.3 & \underline{3.3} & 56.1 & 23.0 & 19.5 & 18.6 & 38.4 & 1.5 & 54.8 & \underline{24.1} & \underline{20.4} & \underline{20.1} & 33.3 & 1.3 & 52.2 & 21.1 & 17.7 & 17.5 \\
 & 32B & 37.5 & \textbf{28} & 51.6 & 20.3 & 18.0 & 16.2 & 27.4 & \textbf{29} & 48.8 & 19.7 & 17.4 & 16.2 & 17.1 & \textbf{35} & 46.5 & 15.1 & 12.8 & 12.2 \\
\midrule
\multicolumn{20}{c}{\textit{VLM Backbone Decoders}} \\
\midrule
\multirow{3}{*}{InternVL} & 1B & 28.9 & 1.4 & \textbf{59.5} & 27.3 & \underline{23.9} & \underline{22.8} & 26.0 & 1.2 & 57.3 & 26.9 & 23.5 & 22.4 & 23.8 & 1.7 & 55.1 & \underline{24.0} & 20.3 & 19.8 \\
 & 2B & 30.7 & \underline{3.9} & 55.1 & 25.1 & 21.5 & 20.2 & 25.7 & 3.9 & 54.8 & 25.8 & 22.8 & 21.4 & 25.3 & \underline{5.5} & 53.4 & \textbf{24.8} & \textbf{21.5} & \textbf{20.7} \\
 & 8B & 39.8 & 3.8 & 58.9 & \textbf{28.8} & \textbf{24.4} & 22.7 & 34.5 & \textbf{5.1} & 55.8 & 26.9 & 23.7 & 22.7 & 19.9 & \textbf{15} & 46.9 & 20.9 & 17.8 & 16.8 \\
LLaVA-1.5 & 7B & \textbf{53.7} & 1.1 & 58.8 & 27.0 & 22.9 & 22.2 & 35.1 & 1.2 & 56.9 & 27.2 & 23.1 & 22.6 & 32.1 & 1.6 & 55.5 & 22.7 & 19.0 & 18.8 \\
LLaVA-1.6-Mistral & 7B & \textbf{53.7} & 1.1 & 59.0 & 25.4 & 21.4 & 20.8 & \textbf{39.5} & 1.1 & 57.5 & 25.3 & 21.9 & 21.6 & \underline{37.2} & 1.3 & \underline{55.7} & 23.1 & 19.4 & 19.2 \\
\multirow{2}{*}{LLaVA-1.6-Vicuna} & 7B & 44.5 & 1.1 & 59.3 & 27.2 & 23.1 & 22.5 & 36.9 & 1.1 & 57.1 & 26.8 & 23.1 & 22.4 & 30.4 & 1.4 & 54.9 & 23.2 & 19.8 & 19.1 \\
 & 13B & \underline{47.2} & 1.0 & 58.8 & 27.1 & 23.4 & \textbf{23.0} & 36.0 & 1.1 & \textbf{59.7} & \textbf{28.9} & \textbf{25.5} & \textbf{25.0} & \textbf{37.5} & 1.0 & \textbf{56.7} & 23.6 & 19.8 & 19.5 \\
\multirow{4}{*}{Qwen3-VL} & 2B & 39.2 & \textbf{5.1} & 57.8 & 25.9 & 21.9 & 20.2 & 34.5 & \underline{4.6} & 57.1 & 26.7 & 22.7 & 22.2 & 25.3 & 1.5 & 54.1 & 24.0 & \underline{20.7} & \underline{20.1} \\
 & 4B & 40.7 & 1.2 & \underline{59.5} & \underline{27.4} & 23.0 & 21.6 & 33.3 & 1.2 & \underline{58.0} & \underline{28.2} & \underline{23.9} & \underline{23.2} & 23.5 & 1.2 & 54.9 & 23.4 & 19.9 & 19.5 \\
 & 8B & 42.2 & 1.4 & 58.8 & 25.7 & 21.8 & 20.0 & 32.4 & 1.8 & 56.5 & 26.9 & 23.0 & 23.0 & 29.5 & 1.3 & 53.9 & 23.0 & 19.7 & 18.6 \\
 & 32B & 45.1 & 2.2 & 58.9 & 25.6 & 21.3 & 19.8 & \underline{38.9} & 1.2 & 57.4 & 27.1 & 23.0 & 22.4 & 33.9 & 2.7 & 54.6 & 22.5 & 18.8 & 18.1 \\
\midrule
\multicolumn{20}{c}{\textit{Standalone LLMs}} \\
\midrule
Mistral & 7B & 0.0 & \textbf{30} & 25.1 & 1.9 & 1.2 & 0.0 & 0.0 & \textbf{30} & 21.8 & 1.8 & 1.7 & 0.0 & 0.0 & \textbf{28} & 17.8 & 1.4 & 0.4 & 0.0 \\
Phi-3.5-mini & 3.8B & 5.6 & \underline{14} & 36.7 & 9.2 & 8.6 & 3.8 & 4.7 & \underline{16} & 36.5 & 11.2 & 9.7 & 6.7 & 9.7 & \underline{15} & 41.4 & 11.8 & 10.1 & 8.0 \\
Qwen2.5-Instruct & 7B & \textbf{35.4} & 1.3 & 54.4 & 21.5 & 17.3 & 16.6 & \textbf{28.3} & 1.6 & \underline{51.3} & 21.3 & 17.2 & 16.8 & \textbf{24.4} & 1.3 & 44.8 & 17.1 & 12.3 & 12.3 \\
\multirow{5}{*}{Qwen3} & 0.6B & 7.7 & 1.1 & 32.2 & 8.6 & 3.4 & 2.4 & 7.7 & 1.2 & 32.6 & 8.1 & 2.2 & 1.7 & 5.7 & 1.3 & 37.6 & 10.2 & 5.2 & 4.4 \\
 & 1.7B & 9.1 & 7.8 & 45.6 & 14.2 & 11.8 & 9.7 & 10.0 & 7.5 & 43.1 & 15.8 & 12.8 & 11.9 & 8.0 & 5.7 & 42.1 & 14.8 & 11.2 & 9.8 \\
 & 4B & 18.0 & 1.3 & \underline{54.4} & \underline{24.7} & \textbf{20.4} & \underline{18.3} & 20.1 & 1.2 & \textbf{52.2} & \textbf{24.2} & \textbf{20.7} & \textbf{20.0} & 18.0 & 1.1 & \textbf{50.1} & \textbf{22.2} & \textbf{19.0} & \textbf{18.1} \\
 & 8B & 27.1 & 2.3 & 52.2 & 21.3 & 16.3 & 14.7 & \underline{21.5} & 1.2 & 51.1 & 21.9 & 18.1 & 17.1 & 15.0 & 1.4 & 44.1 & 16.2 & 11.9 & 10.9 \\
 & 32B & \underline{32.7} & 1.9 & \textbf{54.9} & \textbf{25.2} & \underline{20.3} & \textbf{18.7} & 20.9 & 1.8 & 50.0 & \underline{22.4} & \underline{19.2} & \underline{17.7} & \underline{19.5} & 3.2 & 45.8 & \underline{18.4} & \underline{14.8} & \underline{13.4} \\
\multirow{2}{*}{Vicuna} & 7B & 17.4 & 6.9 & 43.6 & 15.8 & 15.4 & 11.8 & 14.6 & 5.2 & 41.1 & 15.0 & 15.0 & 11.4 & 9.3 & 5.3 & 39.4 & 13.8 & 13.0 & 10.9 \\
 & 13B & 26.8 & 3.0 & 54.4 & 21.8 & 19.4 & 16.1 & 19.5 & 2.9 & 46.7 & 15.6 & 14.4 & 9.8 & 17.3 & 5.1 & \underline{46.6} & 15.9 & 13.9 & 11.3 \\
\midrule
\multicolumn{20}{c}{\textit{Standalone LLMs with Thinking}} \\
\midrule
\multirow{5}{*}{Qwen3} & 0.6B & 7.7 & 1.1 & 32.2 & 8.6 & 3.4 & 2.4 & 7.7 & 1.2 & 32.6 & 8.1 & 2.2 & 1.7 & 5.7 & 1.3 & 37.6 & 10.2 & 5.2 & 4.4 \\
 & 1.7B & 16.7 & \underline{198} & 39.9 & 12.0 & 10.0 & 10.1 & 9.4 & \underline{204} & 39.1 & 13.4 & 11.9 & 11.2 & 11.5 & \underline{230} & 34.9 & 10.1 & 8.2 & 7.9 \\
 & 4B & \underline{18.0} & 1.3 & \underline{50.2} & \underline{20.7} & \underline{16.2} & \underline{14.0} & \underline{20.1} & 1.2 & \underline{46.2} & \underline{18.5} & 15.1 & 14.2 & \textbf{18.0} & 1.1 & \textbf{47.1} & \textbf{16.6} & \textbf{13.9} & \textbf{12.2} \\
 & 8B & \textbf{27.1} & 2.3 & \textbf{52.2} & \textbf{21.3} & \textbf{16.3} & \textbf{14.7} & \textbf{21.5} & 1.2 & \textbf{51.1} & \textbf{21.9} & \textbf{18.1} & \textbf{17.1} & \underline{15.0} & 1.4 & \underline{44.1} & \underline{16.2} & \underline{11.9} & \underline{10.9} \\
 & 32B & 11.2 & \textbf{216} & 42.3 & 16.4 & 15.4 & 13.9 & 10.4 & \textbf{217} & 40.3 & 16.6 & \underline{15.3} & \underline{14.3} & 8.3 & \textbf{252} & 36.8 & 12.2 & 10.5 & 9.9 \\
\bottomrule
\end{tabular}
}
\caption{\small Blind performance across evaluation variants (Original, Weaker, Pronominalized). Acc = VQA accuracy (\%); Len = mean answer length in words; SBERT, chrF, ROUGE-1, Exact = human--model (HM) agreement (\%). Human row shows human--human (HH) agreement.}
\label{tab:comprehensive_inst_blind}
\end{table*}

\begin{table*}[t]
\scriptsize
\centering
\setlength{\tabcolsep}{2.5pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lc|cccccc|cccccc|cccccc}
\toprule
\textbf{Model} & \textbf{Size} & \multicolumn{6}{c|}{\textbf{Original}}& \multicolumn{6}{c|}{\textbf{Weaker}}& \multicolumn{6}{c}{\textbf{Pronominalized}} \\
\cmidrule(lr){3-8}\cmidrule(lr){9-14}\cmidrule(lr){15-20}
 & & \textbf{Acc} & \textbf{Len} & \textbf{SBERT} & \textbf{chrF} & \textbf{ROUGE-1} & \textbf{Exact}& \textbf{Acc} & \textbf{Len} & \textbf{SBERT} & \textbf{chrF} & \textbf{ROUGE-1} & \textbf{Exact}& \textbf{Acc} & \textbf{Len} & \textbf{SBERT} & \textbf{chrF} & \textbf{ROUGE-1} & \textbf{Exact} \\
\midrule
\textbf{Human (N=40)} & -- & 26.3 & 1.3 & 59.4 & 27.5 & 24.6 & 22.6 & 22.9 & 1.3 & 57.8 & 26.9 & 23.7 & 22.4 & 22.6 & 1.3 & 56.0 & 24.4 & 21.1 & 20.2 \\
\midrule
\multicolumn{20}{c}{\textit{Vision-Language Models}} \\
\midrule
\multirow{3}{*}{InternVL} & 1B & 36.9 & 1.2 & \underline{57.7} & 23.5 & 20.1 & 18.5 & 36.6 & 1.2 & 54.9 & 21.9 & 19.1 & 17.9 & 33.6 & 1.2 & \underline{53.6} & \underline{21.8} & \underline{18.7} & \underline{18.0} \\
 & 2B & 44.5 & 1.3 & 55.4 & 21.1 & 17.8 & 15.7 & 38.6 & 1.2 & \underline{55.6} & 22.0 & 19.0 & 17.6 & 39.2 & 1.3 & 53.0 & 20.5 & 17.0 & 16.2 \\
 & 8B & 37.2 & 1.5 & \textbf{61.7} & \textbf{28.1} & \textbf{24.4} & \textbf{21.7} & 33.3 & \underline{1.7} & \textbf{60.1} & \textbf{27.1} & \textbf{24.1} & \textbf{22.7} & 29.5 & \underline{1.7} & \textbf{56.7} & \textbf{23.3} & \textbf{20.3} & \textbf{18.8} \\
LLaVA-1.5 & 7B & 47.2 & 1.0 & 51.9 & 19.0 & 15.5 & 15.0 & \textbf{47.5} & 1.0 & 50.8 & 17.0 & 14.3 & 13.7 & 35.4 & 1.1 & 48.7 & 16.7 & 14.0 & 13.6 \\
LLaVA-1.6-Mistral & 7B & \textbf{53.7} & 1.1 & 55.7 & 22.1 & 18.3 & 17.2 & \underline{46.0} & 1.1 & 53.9 & 20.8 & 17.9 & 17.3 & \underline{41.3} & 1.1 & 50.7 & 18.4 & 15.2 & 14.6 \\
\multirow{2}{*}{LLaVA-1.6-Vicuna} & 7B & 51.6 & 1.0 & 54.5 & 21.2 & 18.1 & 16.7 & 44.2 & 1.0 & 52.3 & 19.2 & 16.1 & 15.3 & \textbf{42.3} & 1.0 & 51.1 & 18.6 & 15.5 & 14.9 \\
 & 13B & \underline{52.4} & 1.0 & 56.8 & 24.3 & 21.1 & \underline{20.3} & 38.6 & 1.1 & 54.5 & 23.0 & 20.1 & 19.4 & 37.5 & 1.0 & 52.0 & 18.8 & 15.7 & 15.3 \\
\multirow{4}{*}{Qwen3-VL} & 2B & 47.6 & 1.3 & 56.1 & 21.9 & 18.2 & 16.3 & 41.9 & 1.3 & 53.6 & 21.0 & 17.3 & 16.4 & 40.2 & 1.3 & 52.8 & 19.4 & 16.1 & 15.4 \\
 & 4B & 50.1 & 1.2 & 56.9 & 23.9 & 19.9 & 18.3 & 37.5 & 1.2 & 55.2 & 24.2 & 20.7 & 19.9 & 38.7 & 1.1 & 52.8 & 21.0 & 18.0 & 17.1 \\
 & 8B & 47.3 & \underline{3.3} & 57.0 & 24.0 & 20.6 & 18.9 & 38.4 & 1.5 & 54.9 & \underline{24.5} & 21.1 & \underline{20.1} & 33.3 & 1.3 & 52.3 & 21.5 & 18.2 & 17.5 \\
 & 32B & 37.5 & \textbf{28} & 56.4 & \underline{25.2} & \underline{22.3} & 19.7 & 27.4 & \textbf{29} & 53.4 & 23.9 & \underline{21.2} & 19.2 & 17.1 & \textbf{35} & 51.5 & 19.5 & 16.4 & 15.4 \\
\midrule
\multicolumn{20}{c}{\textit{VLM Backbone Decoders}} \\
\midrule
\multirow{3}{*}{InternVL} & 1B & 28.9 & 1.4 & 59.5 & 27.6 & \underline{24.7} & 22.8 & 26.0 & 1.2 & 57.3 & 27.0 & 23.9 & 22.4 & 23.8 & 1.7 & 55.3 & 24.4 & 20.8 & 19.8 \\
 & 2B & 30.7 & \underline{3.9} & 55.4 & 25.7 & 22.6 & 20.4 & 25.7 & 3.9 & 55.3 & 26.4 & 23.6 & 21.8 & 25.3 & \underline{5.5} & 54.0 & \underline{25.3} & \textbf{22.2} & \textbf{20.9} \\
 & 8B & 39.8 & 3.8 & \textbf{60.9} & \textbf{30.5} & \textbf{26.5} & \textbf{23.8} & 34.5 & \textbf{5.1} & \underline{59.3} & \textbf{30.1} & \textbf{26.8} & \textbf{25.1} & 19.9 & \textbf{15} & 52.5 & \textbf{25.4} & \underline{21.6} & 20.0 \\
LLaVA-1.5 & 7B & \textbf{53.7} & 1.1 & 58.9 & 27.3 & 23.6 & 22.2 & 35.1 & 1.2 & 57.0 & 27.3 & 23.5 & 22.6 & 32.1 & 1.6 & \underline{56.3} & 23.4 & 19.7 & 19.1 \\
LLaVA-1.6-Mistral & 7B & \textbf{53.7} & 1.1 & 59.0 & 25.7 & 22.0 & 20.8 & \textbf{39.5} & 1.1 & 57.5 & 25.4 & 22.2 & 21.6 & \underline{37.2} & 1.3 & 55.8 & 23.4 & 19.8 & 19.2 \\
\multirow{2}{*}{LLaVA-1.6-Vicuna} & 7B & 44.5 & 1.1 & 59.4 & 27.5 & 23.8 & 22.5 & 36.9 & 1.1 & 57.1 & 26.9 & 23.5 & 22.4 & 30.4 & 1.4 & 55.8 & 23.9 & 20.6 & 19.5 \\
 & 13B & \underline{47.2} & 1.0 & 58.9 & 27.4 & 24.0 & \underline{23.0} & 36.0 & 1.1 & \textbf{59.8} & \underline{29.1} & \underline{25.9} & \underline{25.0} & \textbf{37.5} & 1.0 & \textbf{56.8} & 24.0 & 20.2 & 19.5 \\
\multirow{4}{*}{Qwen3-VL} & 2B & 39.2 & \textbf{5.1} & 58.7 & 26.6 & 23.1 & 20.6 & 34.5 & \underline{4.6} & 57.4 & 27.3 & 23.8 & 22.4 & 25.3 & 1.5 & 54.6 & 24.4 & 21.4 & \underline{20.3} \\
 & 4B & 40.7 & 1.2 & 59.6 & \underline{27.9} & 23.9 & 21.6 & 33.3 & 1.2 & 58.1 & 28.6 & 24.7 & 23.2 & 23.5 & 1.2 & 55.0 & 23.8 & 20.5 & 19.5 \\
 & 8B & 42.2 & 1.4 & \underline{59.7} & 26.6 & 23.0 & 20.3 & 32.4 & 1.8 & 56.7 & 27.3 & 23.7 & 23.0 & 29.5 & 1.3 & 54.7 & 23.6 & 20.4 & 19.0 \\
 & 32B & 45.1 & 2.2 & 59.5 & 26.2 & 22.3 & 20.0 & \underline{38.9} & 1.2 & 57.5 & 27.4 & 23.6 & 22.4 & 33.9 & 2.7 & 55.8 & 23.5 & 20.0 & 18.6 \\
\midrule
\multicolumn{20}{c}{\textit{Standalone LLMs}} \\
\midrule
Mistral & 7B & 0.0 & \textbf{30} & 25.8 & 4.4 & 3.2 & 0.0 & 0.0 & \textbf{30} & 21.3 & 4.0 & 3.7 & 0.0 & 0.0 & \textbf{28} & 20.3 & 2.8 & 0.9 & 0.0 \\
Phi-3.5-mini & 3.8B & 5.6 & \underline{14} & 40.0 & 11.0 & 10.5 & 4.2 & 4.7 & \underline{16} & 40.2 & 13.3 & 11.9 & 7.6 & 9.7 & \underline{15} & 43.5 & 13.1 & 11.4 & 8.3 \\
Qwen2.5-Instruct & 7B & \textbf{35.4} & 1.3 & 54.4 & 21.9 & 18.0 & 16.6 & \textbf{28.3} & 1.6 & \underline{51.2} & 21.6 & 17.7 & 17.0 & \textbf{24.4} & 1.3 & 45.3 & 17.6 & 12.9 & 12.5 \\
\multirow{5}{*}{Qwen3} & 0.6B & 7.7 & 1.1 & 32.2 & 8.7 & 3.5 & 2.4 & 7.7 & 1.2 & 32.4 & 8.1 & 2.3 & 1.7 & 5.7 & 1.3 & 37.7 & 10.4 & 5.4 & 4.4 \\
 & 1.7B & 9.1 & 7.8 & 46.6 & 15.1 & 12.6 & 10.1 & 10.0 & 7.5 & 43.7 & 16.2 & 13.0 & 12.0 & 8.0 & 5.7 & 43.7 & 16.5 & 12.7 & 10.9 \\
 & 4B & 18.0 & 1.3 & 54.4 & \underline{25.0} & \underline{20.9} & \underline{18.3} & 20.1 & 1.2 & \textbf{52.2} & \textbf{24.4} & \textbf{21.1} & \textbf{20.0} & 18.0 & 1.1 & \textbf{50.1} & \textbf{22.4} & \textbf{19.3} & \textbf{18.1} \\
 & 8B & 27.1 & 2.3 & 52.1 & 21.8 & 17.0 & 14.7 & \underline{21.5} & 1.2 & 51.1 & 22.2 & 18.7 & 17.1 & 15.0 & 1.4 & 44.2 & 16.3 & 12.2 & 10.9 \\
 & 32B & \underline{32.7} & 1.9 & \textbf{55.4} & \textbf{25.9} & \textbf{21.4} & \textbf{18.9} & 20.9 & 1.8 & 50.2 & \underline{22.9} & \underline{19.9} & \underline{17.8} & \underline{19.5} & 3.2 & 46.2 & \underline{19.1} & \underline{15.6} & \underline{13.7} \\
\multirow{2}{*}{Vicuna} & 7B & 17.4 & 6.9 & 44.2 & 16.3 & 15.5 & 11.8 & 14.6 & 5.2 & 41.0 & 15.4 & 15.0 & 11.5 & 9.3 & 5.3 & 39.9 & 14.6 & 13.7 & 11.5 \\
 & 13B & 26.8 & 3.0 & \underline{55.1} & 22.5 & 20.5 & 16.3 & 19.5 & 2.9 & 47.1 & 15.9 & 15.1 & 9.8 & 17.3 & 5.1 & \underline{47.7} & 16.8 & 14.7 & 11.5 \\
\midrule
\multicolumn{20}{c}{\textit{Standalone LLMs with Thinking}} \\
\midrule
\multirow{5}{*}{Qwen3} & 0.6B & 7.7 & 1.1 & 32.2 & 8.7 & 3.5 & 2.4 & 7.7 & 1.2 & 32.4 & 8.1 & 2.3 & 1.7 & 5.7 & 1.3 & 37.7 & 10.4 & 5.4 & 4.4 \\
 & 1.7B & 16.7 & \underline{198} & \underline{53.5} & \underline{22.4} & \underline{19.3} & \underline{17.9} & 9.4 & \underline{204} & \textbf{57.7} & \underline{27.5} & \underline{24.1} & \underline{22.7} & 11.5 & \underline{230} & \underline{48.7} & \underline{20.3} & \underline{16.4} & \underline{14.8} \\
 & 4B & \underline{18.0} & 1.3 & 50.2 & 21.0 & 16.8 & 14.0 & \underline{20.1} & 1.2 & 46.2 & 18.7 & 15.5 & 14.2 & \textbf{18.0} & 1.1 & 47.1 & 16.8 & 14.1 & 12.2 \\
 & 8B & \textbf{27.1} & 2.3 & 52.1 & 21.8 & 17.0 & 14.7 & \textbf{21.5} & 1.2 & 51.1 & 22.2 & 18.7 & 17.1 & \underline{15.0} & 1.4 & 44.2 & 16.3 & 12.2 & 10.9 \\
 & 32B & 11.2 & \textbf{216} & \textbf{60.0} & \textbf{31.6} & \textbf{29.6} & \textbf{26.7} & 10.4 & \textbf{217} & \underline{56.1} & \textbf{31.3} & \textbf{29.2} & \textbf{26.6} & 8.3 & \textbf{252} & \textbf{54.9} & \textbf{27.2} & \textbf{23.6} & \textbf{22.0} \\
\bottomrule
\end{tabular}
}
\caption{\small Blind performance across evaluation variants (Original, Weaker, Pronominalized). Acc = VQA accuracy (\%); Len = mean answer length in words; SBERT, chrF, ROUGE-1, Exact = human--model (HM) agreement (\%). Human row shows human--human (HH) agreement. All answers are length-normalized ($\leq$5 words) before scoring.}
\label{tab:comprehensive_inst_blind_w5}
\end{table*}

\section{Blind Awareness Instruction Effect}
\label{app:inst}

The explicit blind-awareness instruction was: \textit{``Note: No images are provided. For each
question, imagine an appropriate image exists and answer based on the most
common or universal scenario.''}

Instruction changes accuracy by only a few points, confirming that it does
not reliably improve performance even when it substantially changes answer
behavior.

\section{ChrF Agreement by Operation Type (Human--Human)}
\label{app:chrf_human}

ChrF measures character $n$-gram overlap and captures surface-level
lexical similarity rather than semantic similarity. Unlike SBERT, HH
chrF shows no significant operation-type effect in any variant
(ANOVA $F{=}0.83$, $p{=}0.59$ for the original formulation; $F{=}1.08$, $p{=}0.39$
for the weaker-object formulation; $F{=}0.98$, $p{=}0.46$ for the pronominalized formulation). Entity group is also
non-significant across all variants ($p{>}0.36$). This null result
confirms that the operation-type structure found in HH SBERT
(Section~\ref{sec:human_patterns}) is semantic: humans answering the
same question type give \emph{semantically} similar responses (captured
by SBERT) even when they use different surface forms (missed by chrF).
For the ANOVA-style comparisons reported in the paper, we checked the
homogeneity-of-variance assumption and also verified that Welch-style
variance-robust tests lead to the same qualitative conclusions. We therefore
report the standard ANOVA statistics in the main text for readability, while
interpreting them together with practical effect-size estimates ($\eta^2$)
rather than $p$-values alone.
\begin{table}[t]
\centering
\caption{Effect-size summary for human--human SBERT group differences across control variants ($q{=}113$). $\eta^2$ is reported alongside one-way ANOVA and Kruskal--Wallis statistics. Operation effects are moderate-to-large across all variants, while entity effects remain negligible.}
\label{tab:human_hh_effect_sizes}
\small
\begin{tabular}{llrrrrr}
\toprule
Dimension & Variant & $N$ & Groups & $F$ & $H$ & $\eta^2$ \\
\midrule
Operation & Original & 113 & 11 & 5.75 & 41.82 & 0.361 \\
Operation & Weaker & 113 & 11 & 5.93 & 41.44 & 0.368 \\
Operation & Pronominalized & 113 & 11 & 6.60 & 41.51 & 0.393 \\
Entity & Original & 113 & 5 & 0.92 & 3.49 & 0.033 \\
Entity & Weaker & 113 & 5 & 0.68 & 2.56 & 0.024 \\
Entity & Pronominalized & 113 & 5 & 0.19 & 0.53 & 0.007 \\
\bottomrule
\end{tabular}
\end{table}

% \section{Corpus Frequency Comparison}
% \label{app:corpus_freq}
%
% \begin{figure}[t]
% \centering
% \includegraphics[width=\columnwidth]{comparison_yn_q388}
% \vspace{0.3em}
% \includegraphics[width=\columnwidth]{comparison_number_q122}
% \caption{Blind model answer distributions (group means) vs.\ VQA~v2.0
% training-set frequencies. \textbf{Top:} Yes/no---training is balanced
% (51/49) but VLMs default to ``no'' (70\%); backbone decoders flip to
% ``yes'' (70\%). \textbf{Bottom:} Number---training mode is ``2--3''
% (34\%), yet VLMs collapse to ``0'' (66\%).}
% \label{fig:corpus_freq}
% \end{figure}

% \section{Agreement by Control Variant --- All Metrics}
% \label{app:agreement_variant}

% Figures~\ref{fig:av_simcse}--\ref{fig:av_exact} extend
% Figure~\ref{fig:variants} (right) to SimCSE, chrF, and exact match,
% confirming that content-noun anchor removal reduces human--model alignment
% independently of the metric used.

% \begin{figure}[t]
% \centering
% \includegraphics[width=\columnwidth]{inst_blind_simcse_q113_h40_yesno_7b}
% \caption{Human--model SimCSE cosine across control variants (7/8B, $q=113$).}
% \label{fig:av_simcse}
% \end{figure}

% \begin{figure}[t]
% \centering
% \includegraphics[width=\columnwidth]{inst_blind_chrf_q113_h40_yesno_7b}
% \caption{Human--model chrF across control variants (7/8B, $q=113$).}
% \label{fig:av_chrf}
% \end{figure}

% \begin{figure}[t]
% \centering
% \includegraphics[width=\columnwidth]{inst_blind_exact_q113_h40_yesno_7b}
% \caption{Human--model exact-match rate across control variants (7/8B, $q=113$).}
% \label{fig:av_exact}
% \end{figure}

\section{Agreement by Scale and Variant}
\label{app:scale_variant}

Figure~\ref{fig:scale_variant} extends the scale analysis
across all three control variants. As entity
anchors are removed (original $\to$ weaker-object $\to$ pronominalized), the human--human ceiling drops modestly
while model groups show diverging sensitivity: backbone decoders remain near
the ceiling throughout, VLMs show mild degradation, and standalone LLMs fall
further below the human reference as lexical anchors are removed. Scale alone
does not predict degradation, and family-specific outliers remain visible. This
matched subset excludes Mistral-7B and Phi-3.5-mini to avoid known blind-answer
outliers from dominating the small standalone-LLM comparison.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{scale_metrics_vABC/inst_blind_agreement_groups_sbert_vCBA_q113_h40_yesno}
\caption{SBERT human--model agreement by parameter count across control
variants. Left: original, center:
weaker-object, right: pronominalized. Each point is one model; trend lines
show group-level log-linear fits.}
\label{fig:scale_variant}
\end{figure*}

\section{Per-Family Alignment Structure}
\label{app:family_alignment}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{inst_blind_sbert_family_vABC_q113_yesno}
\caption{Per-family SBERT human--model agreement across control variants.
Row~1: VLMs (solid) and backbone decoders (dotted).
Row~2: standalone LLMs and think variants.
Right column: group-mean summary with 95\% CI. Line weight scales with parameter count.}
\label{fig:family}
\end{figure*}

Figure~\ref{fig:family} decomposes the group-level trend into individual
model families. In Row~1, backbone decoders consistently match or exceed
their paired VLMs within each family, with the gap widest for InternVL
(backbone 8B exceeds VLM 8B by $+$0.03--0.05 SBERT). Qwen3-VL-32B is a
notable outlier: the largest VLM shows the \emph{lowest} alignment in its
family, suggesting that scale amplifies vision-encoder interference under
blind conditions. In Row~2, Qwen3 standalone LLMs show clear scale
ordering (0.6B at bottom, 4--8B at top), while Mistral-7B is
a persistent outlier with the lowest agreement of any model ($\sim$0.18--0.25),
consistent with its high verbosity (mean 29.7 words per answer).
Mistral-7B achieves 0\% VQA accuracy across all 339 question--variant
responses, yet this is partly a scoring artifact of its verbosity rather
than a complete failure to exploit priors.
Although approximately 48\% of its responses contain plausible
prior-based guesses (e.g., ``baggage claim area\ldots{} where luggage
is retrieved after arriving at airport'' for GT: \textit{airport};
``ketchup and mustard are commonly used condiments'' for GT:
\textit{mustard, ketchup}), these answers are embedded in
multi-sentence elaborations (97\% of answers exceed 30 words) that
prevent VQA-standard matching from extracting them.
The remaining 52\% are explicit refusals.
It is therefore excluded from the variant ladder plots in
Figure~\ref{fig:scale_variant}, where it would appear as a
flat line at zero.

\section{Grouped Operation and Entity Patterns}
\label{app:grouped_patterns}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{hm_sbert_entity_op_grouped_variants_7b}
\caption{Human--model SBERT by entity and operation groups (7--8B scale).
Bars show group means across model types (VLM, backbone decoder, standalone LLM);
error bars indicate variant spread (min--max across Original/Weaker/Pronominalized);
dotted lines mark the HH baseline (mean across variants).
Individual model markers are overlaid. Identity and action show some of the
clearest backbone--VLM separation, while count remains a low-agreement region.}
\label{fig:op_hm}
\end{figure*}

\input{tables/group_corr_op}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{question_diagnostic/hh_vs_hm_group_scatter_7b_op}
\caption{Per-model group-level scatter: HH vs.\ HM SBERT aggregated by operation type.
Each point is one operation group under one variant; text labels identify the group.
Spearman $\rho$, cosine similarity, and rmcorr are shown per panel.}
\label{fig:group_scatter_op}
\end{figure*}

\input{tables/group_corr_ent}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{question_diagnostic/hh_vs_hm_group_scatter_7b_ent}
\caption{Per-model group-level scatter: HH vs.\ HM SBERT aggregated by entity type.
Layout mirrors Figure~\ref{fig:group_scatter_op}; entity labels annotate each point.}
\label{fig:group_scatter_ent}
\end{figure*}

\section{Answer Distribution Biases}
\label{app:distributions}

\begin{figure*}[t]
\centering
\includegraphics[width=0.49\textwidth]{inst_blind_combined_yesno_abstsplit}%
\hfill%
\includegraphics[width=0.49\textwidth]{inst_blind_combined_number_abstsplit}
\caption{All-model blind answer distributions (yes/no left, count right).
Ground truth uses all 10 per-question annotator responses.
The backbone$>$VLM pattern from Figure~\ref{fig:dist_main} persists across
all families; standalone LLMs show substantial abstention and format mismatch.}
\label{fig:dist}
\end{figure*}

Three systematic biases emerge in the combined distribution view:
\textbf{No-bias} in yes/no: full VLMs default to narrower yes/no priors that
depart from the human distribution, while backbone decoders move back toward
the human split.
\textbf{Zero-bias} in count: full VLMs collapse toward a small subset of
numeric defaults, whereas humans distribute across plausible small counts.
\textbf{Abstention} in standalone LLMs: they often fail to inhabit the VQA
answer distribution at all, instead hedging or abstaining.
\begin{table*}[t]
\centering
\caption{Identity-focused human examples with the largest human degradation under pronominalization. These cases highlight where human agreement itself collapses most strongly, making identity the most fragile operation under referential underspecification.}
\label{tab:identity_focus_examples}
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{llrrrrll}
\toprule
Question & Entity & HH$_C$ & HH$_A$ & $\Delta_{C\to A}^{HH}$ & HH-HM & Human top & Model top \\
\midrule
What animal is this cake meant to represent? & food & 0.536 & 0.199 & 0.336 & 0.070 & cat (9); dog (8); rabbit (7) & dog (3); cat (2); bear (2) \\
What type of bear is this? & animal & 0.674 & 0.341 & 0.332 & 0.035 & brown bear (10); polar bear (9); asian black bear (8) & grizzly bear (4); black (3); brown (2) \\
What type of shoe is that? & product & 0.653 & 0.335 & 0.318 & 0.033 & sneakers (13); slippers (5); sandals (4) & sneaker (12); sneakers (1) \\
What is this player's position called? & person & 0.574 & 0.337 & 0.237 & 0.112 & striker (9); Goalkeeper (6); midfielder (5) & goalie (6); goalkeeper (2); outfielder (2) \\
What type of bread does this appear to be? & food & 0.526 & 0.316 & 0.210 & 0.055 & sandwich bread (9); baguette (8); salt bread (2) & white bread (4); wheat (3); sourdough (3) \\
\bottomrule
\end{tabular}
}
\end{table*}

% Correlation with VQA~v2.0 training frequencies is weak or negative for VLMs,
% suggesting VLM blind biases are uncertainty defaults shaped by uninformative
% visual input rather than training-frequency parroting.

\section{Abstention and Calibration Ablations}
\label{app:abstention_calibration}

This section presents robustness analyses for the blind
setting: abstention behavior and instruction-induced abstention collapse.
Together they clarify that the backbone
advantage is not an artifact of refusal behavior.

\subsection{Instruction Gating Effect}
\label{app:inst_gating}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{merged_abstention_vC_q113}
\caption{Soft and hard abstention rates (blind $\to$ explicit blind-awareness instruction) per model.
Standalone LLMs show large shifts; VLMs/backbones are nearly unchanged.}
\label{fig:abstention_shift_models}
\end{figure}

For reference, human explicit abstention is negligible (0.3\% overall), so
the abstention patterns below are primarily a model-side phenomenon rather
than a general property of blind answering.

Instruction (blind $\to$ blind with awareness instruction) increases human--model agreement most on
original questions ($+$0.020 SBERT) and attenuates as lexical anchors are
removed (pronominalized: $+$0.012). The behavioral mechanism is abstention
collapse: standalone LLMs shift from $\sim$30\% soft abstention to near
zero (Figure~\ref{fig:abstention_shift_models}), while VLMs and backbones barely
change---indicating their priors are expressed unconditionally. Accuracy
does not improve (Appendix~\ref{app:inst}), confirming that instruction
primarily converts refusal into committed hallucination rather than grounded
reasoning.

% \subsection{Confidence Calibration}
% \label{app:calibration}
%
% All model groups are severely overconfident.
% VLMs and backbones cluster at high token probabilities (0.8--0.95) regardless
% of correctness. Standalone LLMs show higher ECE (0.642) and erratic curves,
% reflecting incoherent prior activation. This calibration failure is invisible
% to accuracy-only evaluation. Taken together with the abstention analysis
% above, these results show two complementary failure modes: standalone LLMs
% are more refusal-prone before instruction, while VLMs/backbones are more
% likely to produce confident committed answers even under severe visual
% deprivation.

\section{Entropy Analysis}
\label{app:case_study}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{question_diagnostic/entropy_alignment_analysis}
\caption{Information-theoretic analysis (free-text questions, original formulation).
\textbf{Left:} Human vs model answer entropy (bits); points near the
diagonal indicate matched uncertainty. \textbf{Center:} Higher human
uncertainty correlates with lower human--model alignment. \textbf{Right:} Entropy
mismatch (model $-$ human) vs original$\to$pronominalized degradation.}
\label{fig:entropy}
\end{figure*}

Figure~\ref{fig:entropy} provides the information-theoretic complement.
Human and model answer entropies are positively correlated, confirming
that both are more uncertain on the same questions. However, models
systematically show lower entropy than humans---they are more
concentrated on a single answer even when humans distribute across
alternatives. This entropy gap is largest on entity-grounded questions
where models exploit a dominant corpus-frequency prior (e.g., ``sneaker''
for shoe type, ``0'' for count questions).

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{question_diagnostic/hh_vs_hm_scatter_7b}
\caption{Per-question HH vs.\ HM SBERT scatter for the matched 7--8B model set.
Each panel corresponds to one model; colors indicate control variants. Models
closer to the diagonal more faithfully preserve the human question-level
agreement structure.}
\label{fig:hh_hm_scatter_app}
\end{figure*}

\begin{table*}[t]
\centering
\caption{Per-model correlation summary for Figure~\ref{fig:hh_hm_scatter}. Pearson correlation is computed between per-question HH SBERT and HM SBERT across the matched 7/8B models. Variant columns report the same correlation within each control formulation. The misprocessed think variant is excluded.}
\label{tab:hh_hm_scatter_corr}
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{llrrrrrr}
\toprule
Model & Group & All $r$ & $p$ & Original & Weaker & Pronominalized & $N$ \\
\midrule
InternVL-8B & VLM & 0.935 & 9.4e-150 & 0.931 & 0.932 & 0.944 & 331 \\
Qwen3-VL-8B & VLM & 0.895 & 5.2e-119 & 0.892 & 0.889 & 0.904 & 335 \\
LLaVA-1.5-7B & VLM & 0.872 & 2.4e-106 & 0.853 & 0.856 & 0.902 & 339 \\
LLaVA-Mistral & VLM & 0.879 & 4.9e-110 & 0.878 & 0.868 & 0.890 & 338 \\
LLaVA-Vicuna & VLM & 0.884 & 8.4e-113 & 0.882 & 0.882 & 0.886 & 338 \\
InternVL-8B (LM) & Backbone decoder & 0.896 & 1.3e-108 & 0.886 & 0.913 & 0.888 & 304 \\
Qwen3-VL-8B (LM) & Backbone decoder & 0.890 & 1.9e-115 & 0.858 & 0.892 & 0.916 & 334 \\
LLaVA-1.5 (LM) & Backbone decoder & 0.921 & 2.0e-138 & 0.917 & 0.907 & 0.937 & 336 \\
LLaVA-Mistral (LM) & Backbone decoder & 0.928 & 4.6e-146 & 0.918 & 0.926 & 0.937 & 338 \\
LLaVA-Vicuna (LM) & Backbone decoder & 0.918 & 1.5e-136 & 0.925 & 0.913 & 0.918 & 336 \\
Qwen3-8B & Standalone LLM & 0.581 & 5.5e-32 & 0.533 & 0.641 & 0.564 & 339 \\
Qwen2.5-7B-Instruct & Standalone LLM & 0.699 & 1.9e-50 & 0.747 & 0.649 & 0.704 & 335 \\
Vicuna-7B & Standalone LLM & 0.671 & 1.5e-44 & 0.669 & 0.619 & 0.724 & 330 \\
Mistral-7B & Standalone LLM & -0.185 & 4.2e-02 & -0.108 & -0.212 & -0.243 & 121 \\
\bottomrule
\end{tabular}
}
\end{table*}

\input{tables/results_winner_summary}

\end{document}
