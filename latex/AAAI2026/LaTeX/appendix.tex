\appendix
\clearpage

\renewcommand{\thefigure}{A\arabic{figure}}
\renewcommand{\thetable}{A\arabic{table}}
\setcounter{figure}{0}
\setcounter{table}{0}

\section{Dataset and Human Study Details}
\label{app:data}

\subsection{Question Taxonomy}
\label{app:taxonomy}

Table~\ref{tab:taxonomy} lists all operation and entity categories used for group-level analysis.

\begin{table*}[ht]
\centering
{\fontsize{9}{10}\selectfont
\renewcommand{\arraystretch}{0.95}
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\textwidth}{@{}p{0.16\textwidth}cXp{0.38\textwidth}@{}}
\toprule
\textbf{Category} & \textbf{\textit{N}} & \textbf{Description} & \textbf{Example} \\
\midrule
\rowcolor{gray!12}
\multicolumn{4}{c}{\textbf{\textit{Operations}}} \\
\midrule
\rowcolor{gray!6}
Attribute       & 41 & Color, material, type, age, name, or location. & \textit{What type of computer is the cat using?} \\
Count           & 22 & Quantity or number of entities. & \textit{How many benches are in the background?} \\
\rowcolor{gray!6}
Spatial         & 12 & Relative position, direction, or location. & \textit{Which way is the arrow pointing?} \\
Identity        & 9  & What or who a specific entity is. & \textit{What type of bear is this?} \\
\rowcolor{gray!6}
Action          & 7  & What an entity is doing or engaged in. & \textit{What is the elephant eating?} \\
Existence       & 7  & Whether an entity is present in the scene. & \textit{Is there a couch?} \\
\rowcolor{gray!6}
Text Reading    & 5  & Reading visible writing, signs, labels, or logos. & \textit{What does the shirt say?} \\
World Knowledge & 4  & Factual or commonsense knowledge. & \textit{What religion is this building associated with?} \\
\rowcolor{gray!6}
Temporal        & 3  & Duration, age, or temporal ordering of events. & \textit{How far do the girls have to run?} \\
Comparison      & 2  & Comparative or superlative judgment. & \textit{What would these two do if the sun got too bright?} \\
\rowcolor{gray!6}
Causality       & 1  & Reason or causal explanation. & \textit{Why is the man wearing protective gear?} \\
\midrule
\rowcolor{gray!12}
\multicolumn{4}{c}{\textbf{\textit{Entities}}} \\
\midrule
\rowcolor{gray!6}
Object      & 27 & General physical objects. & \textit{Which way is the arrow pointing?} \\
Person      & 26 & Human subjects, including people, faces, and body parts. & \textit{Which hand is holding the thing?} \\
\rowcolor{gray!6}
Animal      & 17 & Non-human living creatures. & \textit{What color is this horse's head and tail hair?} \\
Food        & 16 & Food and drink items, including prepared dishes. & \textit{What vegetable is in the bowl?} \\
\rowcolor{gray!6}
Other       & 9  & Questions that do not fit any other entity category. & \textit{What kind of information does the book contain?} \\
Place       & 5  & Locations, scenes, settings, or environments. & \textit{What is the name of the store closest to the van?} \\
\rowcolor{gray!6}
Product     & 5  & Manufactured objects such as furniture, tools, or appliances. & \textit{What type of computer is the cat using?} \\
Vehicle     & 4  & Cars, buses, bicycles, aircraft, or watercraft. & \textit{What formation are the aircraft in?} \\
\rowcolor{gray!6}
Text        & 4  & Visible written content such as signs, labels, or logos. & \textit{What language is the writing at the top of the bus in?} \\
\bottomrule
\end{tabularx}
}
\caption{Question taxonomy used for group-level analysis. For anchoring concordance analysis (main paper), low-frequency operation groups (Causality, Comparison, Temporal, Text Reading, World Knowledge; each $n{\leq}5$) are excluded, and entity groups Place, Product, Vehicle, and Text are merged into Other, yielding 7 operation and 5 entity groups with balanced counts.}
\label{tab:taxonomy}
\end{table*}

\subsection{Human-Study Subset Composition}
\label{app:sample_composition}

% Table~\ref{tab:q113_sample_composition} summarizes the 113-question subset used
% for the human study and matched human--model comparisons. The subset spans all
% three answer formats and preserves broad coverage across both operation type and
% entity type.

\input{tables/sample_q113_composition}

\subsection{Control Question Extraction Pipeline}
\label{app:ctl}

The generation pipeline produces four variants per question, but human and model comparisons use three: \textbf{Original} (C, unmodified), \textbf{Weaker} (B, \texttt{weaker\_object}), and \textbf{Pronominalized} (A, \texttt{pronominalized}). The \texttt{deictic\_removed} and \texttt{object\_removed} steps are intermediate scaffolding used by the extraction pipeline but excluded from analysis.

Control variants are extracted using GPT-4.1-mini-2025-04-14 (temperature~0,
seed~42, JSON schema mode) in batches of 50 questions per API call.
The output schema enforces
\texttt{\{deictic\_removed, object\_removed, weaker\_object, pronominalized\}}
as required string fields with \texttt{additionalProperties:~false}.
The system prompt is reproduced verbatim below:

\begin{quote}\small
Generate 4 control variants for each VQA question.
Return ONLY a JSON object \texttt{\{"results":[...]\}}; preserve input order.

\textbf{Rules:}

1) \textit{deictic\_removed}: Remove deictic words (this/that/these/those/here/there)
and spatial markers (left/right/top/bottom/foreground/background) while preserving
grammar. Restructure the sentence if needed to remain fluent.

2) \textit{object\_removed}: Replace the specific object noun with neutral placeholder
``object''. Do not replace pronouns or generic nouns already present.

3) \textit{weaker\_object}: Replace the specific object noun with a broader hypernym
one level up (dog$\to$animal, car$\to$vehicle, drink$\to$beverage, shirt$\to$clothing,
apple$\to$fruit, knife$\to$utensil, sofa$\to$furniture). If no clear hypernym
exists, use ``object''.

4) \textit{pronominalized}: Replace ALL content noun phrases with minimal pronouns.
Grammar-aware rules: attribute (``What color is the dog?'') $\to$ ``What color is it?'';
count (``How many cats are there?'') $\to$ ``How many are there?'';
location (``Where is the phone?'') $\to$ ``Where is it?'';
action (``What is the man doing?'') $\to$ ``What is it doing?'';
yes/no (``Is the dog affectionate?'') $\to$ ``Is it affectionate?'';
multi-NP (``between the girl and the giraffe'') $\to$ ``between them''.
Keep ONLY question words, copula, prepositions, predicate/attribute, and pronouns.
Use ``it''/``they''/``them''---never ``the object''.
\end{quote}


\subsection{Question Taxonomy Extraction}
\label{app:sem_extraction}

Each question in the pool is annotated along two axes --- \emph{operation type} (what reasoning the question demands) and \emph{entity type} (what semantic field its subject belongs to) --- using GPT-4.1-mini-2025-04-14 (temperature~0, seed~42, JSON schema mode).
The output schema enforces \texttt{op} and \texttt{ent} as required enumerated string fields.
% Annotations were extracted in batches of 50 questions per API call with \texttt{additionalProperties:~false} enforced throughout.
The system prompt is reproduced verbatim below:

\begin{quote}\small
Extract VQA question semantics from text only; do not invent visual details.
Return ONLY a JSON object \texttt{\{"results":[...]\}} matching the schema exactly; preserve input order.

\textbf{Decision rules:}

1) \textit{op} priority (highest to lowest):
\texttt{text} (reading cue: written/word/number/say/spell/label/sign/license; brand/logo only if implied visible/printed) $>$
\texttt{count} (how many/number of) $>$
\texttt{spat} (explicit left/right/top/bottom/foreground/background; where-questions; relations like behind/next to) $>$
\texttt{temp} (how long/how old/aged/before/after) $>$
\texttt{cause} (why/reason) $>$
\texttt{comp} (comparative/superlative) $>$
\texttt{attr} (color/material/type/age/duration/name/location asked) $>$
\texttt{exist} (only existential presence: ``is/are there'', ``any'', ``present'') $>$
\texttt{ident} (who/what/which entity is) $>$
\texttt{act} (activity/doing) $>$
\texttt{know} (world knowledge beyond simple visual attributes, when no text-reading cue) $>$ \texttt{other}.

2) If \texttt{w=yesno} AND \texttt{op=exist} $\Rightarrow$ \texttt{ans=bool}, \texttt{space=bin}. Other yes/no use \texttt{op} based on content.

3) \texttt{attr} consistency: color$\to$\texttt{ans=color}, \texttt{space=cat}; material$\to$\texttt{mat,cat}; age$\to$\texttt{age,num}; duration$\to$\texttt{dur,num}; count$\to$\texttt{num,num}; name$\to$\texttt{text,cat}; location$\to$\texttt{loc,cat}; type/ident$\to$\texttt{cat,cat}.

4) \texttt{txt=true} iff \texttt{op=text}. \texttt{know=true} iff \texttt{op=know}.

5) \texttt{sp}/\texttt{rel}/\texttt{dx} only if explicit in question.

6) If \texttt{w=where} and \texttt{op} not in \{\texttt{text,know}\} $\Rightarrow$ \texttt{op=spat}, \texttt{attr=loc}, \texttt{ans=loc}, \texttt{space=cat}.
\end{quote}

\subsection{Human Study Protocol and Statistics}
\label{app:human}

A total of 40 participants were included. Participants were recruited via
online student community forums at universities. All procedures were conducted
under an IRB-approved protocol adhering to the Declaration of Helsinki.
Informed consent was obtained prior to participation.

Participants answered text-only blind-instruction questions across the original, weaker-object, and pronominalized formulations in shuffled order ($N{=}339$ per participant).
Participants were compensated at 10,000~KRW per hour.

Summary statistics are reported in Section~\ref{sec:human_data}.
Figure~\ref{fig:human_stats} shows the full
response-time, answer-length, abstention, and confidence distributions.
Per-participant median RTs range from 2.6~s to 7.5~s with no extreme outliers.
Confidence ranges from 1.5 to 4.7 per participant (mean 3.07), with
per-variant distributions nearly identical---suggesting question surface form
does not systematically inflate or suppress self-reported certainty.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{human_study_statistics}
\caption{Participant response statistics across 40 participants and 15,336 responses.
\textbf{A--B}: Response time distribution and per-participant medians (capped at 99th pct = 35.8~s).
\textbf{C}: Answer length---92.6\% are single-word responses.
\textbf{D}: Confidence rating distribution by variant (nearly uniform across variants).
\textbf{E}: Soft abstention rate ($<$0.04\% per variant) confirms engaged responding.
\textbf{F}: Per-participant mean confidence, sorted---full range used with no ceiling/floor effect.}
\label{fig:human_stats}
\label{fig:human_stats_conf}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{confidence/human_ecdf}
\caption{ECDF of human self-reported confidence (normalised to $[0,1]$) by question
variant. The three curves are nearly identical, with only a modest downward shift
under pronominalization, confirming that human confidence is robust to lexical-anchor
removal and more stable across variants than model token confidence.}
\label{fig:human_ecdf}
\end{figure}

\subsection{Answer Preprocessing Pipeline}
\label{app:preprocessing}

All responses---human and model---go through the same three-step pipeline:

\textbf{(1) Translation.}
Korean responses are translated to English via GPT-4o-mini using the following prompt:
\begin{quote}\small
\textit{You are a precise translation assistant for VQA data.
Task: Translate the Korean answer to English. Return ONLY the translated answer, nothing else.
Rules: Keep answer concise (similar length to original); use natural English phrasing; if it's a number, color, or simple noun, translate directly; do not add explanations.
QUESTION: \{question\}~~KOREAN ANSWER: \{answer\}~~English:}
\end{quote}

\textbf{(2) VQA normalization.}
Answers are lowercased, punctuation stripped, and articles removed following the standard VQA v2 pipeline \citep{antol2015vqa}.

\textbf{(2b) Verbose response stripping.}
Before normalization, model outputs are cleaned with a hand-crafted cascade applied in order:
(i)~\textit{Think-trace removal}: \texttt{<think>...</think>} blocks are stripped via regex; if reasoning text leaks into the output (e.g.\ the response begins \textit{``Okay let's see\ldots''} or \textit{``Hmm, the user is asking\ldots''}), a recovery pass extracts the final committed answer from explicit cues such as \textit{``the answer is\ldots''} or \textit{``therefore X is the answer''}.
(ii)~\textit{Preamble stripping}: leading wrappers matching patterns such as \textit{``The answer is\ldots''}, \textit{``My final answer is\ldots''}, and \textit{``Answer to the question is\ldots''} are removed, as is any residual \texttt{\textbackslash boxed\{\}} template marker.
This cascade is applied uniformly to all model outputs before the normalization step.

\textbf{(3) Abstention cleaning.}
The shared abstention classifier (Appendix~\ref{app:abstention_classifier}) is applied uniformly to both model and human answers to flag non-substantive responses for exclusion from alignment metrics.
Human abstention after normalization is negligible ($<$0.01\% overall; effectively absent), so HH pairs are minimally affected; filtering primarily removes model-abstaining HM pairs.

\subsection{Question Embedding Coverage}
\label{app:tsne}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{subset_tsne_entities}\\[0.4em]
\includegraphics[width=\linewidth]{subset_tsne_operations}
\caption{t-SNE of the 1{,}000-question pool (gray) with the 113-question
human-study subset highlighted. \textbf{Top:} colored by entity type.
\textbf{Bottom:} colored by operation type. Cluster centroids are marked;
the nearest question to each centroid provides a representative example
for the corresponding taxonomy category (Table~\ref{tab:taxonomy}).
The subset spans the full embedding space without concentrating in any
narrow semantic region.}
\label{fig:tsne}
\end{figure}


\clearpage
\section{Model Inventory}
\label{app:model_inventory}

\begin{table*}[ht]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{Model inventory (temperature 0). Each VLM is also evaluated as a
  \textbf{backbone decoder} (text-only, no image tokens, identical weights).
  Standalone Qwen3 models are additionally evaluated with thinking traces enabled
  (same weights; decoding mode differs only).
  $\dagger$~=~excluded from main analysis (incomplete inference data).}
\label{tab:model_inventory}
\begin{tabularx}{\textwidth}{@{}llllX@{}}
\toprule
\textbf{Model} & \textbf{Size} & \textbf{LLM Decoder} & \textbf{Vision Encoder} & \textbf{Notes} \\
\midrule
\multicolumn{5}{c}{\textit{Vision-Language Models}} \\
\midrule
InternVL3.5        & 1B   & Qwen3-0.6B         & InternViT-300M      & Multi-stage contrastive + generative training; InternViT aligned to Qwen3 backbone via dynamic-resolution tiling \citep{zhu2025internvl3}. \\
InternVL3.5        & 2B   & Qwen3-1.7B         & InternViT-300M      & Same training recipe as 1B; larger Qwen3 decoder improves language grounding \citep{zhu2025internvl3}. \\
InternVL3.5        & 8B   & Qwen3-8B           & InternViT-6B        & Uses the full InternViT-6B encoder; strongest VLM in this family on visual grounding tasks \citep{zhu2025internvl3}. \\
LLaVA-1.5          & 7B   & Vicuna-7B v1.5     & CLIP ViT-L/14@336   & Two-stage visual instruction tuning with MLP projection; trained on LLaVA-665K dataset \citep{liu2023visual}. \\
LLaVA-1.6-Mistral  & 7B   & Mistral-7B         & CLIP ViT-L/14@336   & Extends LLaVA-1.5 with AnyRes high-resolution tiling and richer instruction data for improved OCR and reasoning \citep{liu2024llavanext}. \\
LLaVA-1.6-Vicuna   & 7B   & Vicuna-7B v1.5     & CLIP ViT-L/14@336   & Same AnyRes architecture as LLaVA-1.6-Mistral with Vicuna backbone \citep{liu2024llavanext}. \\
LLaVA-1.6-Vicuna   & 13B  & Vicuna-13B v1.5    & CLIP ViT-L/14@336   & Larger Vicuna backbone under the same LLaVA-1.6 recipe \citep{liu2024llavanext}. \\
Qwen3-VL           & 2B   & Qwen3-2B           & Qwen3-ViT           & Native dynamic-resolution vision encoder; trained on large-scale multimodal web data with long-context support \citep{bai2025qwen3vl}. \\
Qwen3-VL           & 4B   & Qwen3-4B           & Qwen3-ViT           & Same architecture as Qwen3-VL-2B; stronger instruction following at 4B scale \citep{bai2025qwen3vl}. \\
Qwen3-VL           & 8B   & Qwen3-8B           & Qwen3-ViT           & Primary Qwen3-VL model in our analysis; competitive with much larger models on standard benchmarks \citep{bai2025qwen3vl}. \\
\midrule
\multicolumn{5}{c}{\textit{Standalone LLMs (Qwen3 also evaluated with thinking traces enabled; same weights)}} \\
\midrule
Mistral-7B          & 7B   & \multicolumn{2}{l}{—} & Sliding-window attention with grouped-query attention; instruction-tuned on proprietary data \citep{jiang2023mistral}. \\
% Phi-3.5-mini removed: 100% verbose outputs, not paired with any VLM backbone, outside the 7-8B matched scale
Qwen2.5-Instruct    & 7B   & \multicolumn{2}{l}{—} & Instruction-tuned on 18T tokens with RLHF; strong general-purpose baseline from the Qwen2.5 family. \\
Qwen3               & 0.6B, 1.7B, 4B, 8B & \multicolumn{2}{l}{—} & Dense models supporting both thinking and non-thinking decoding modes via the same checkpoint; trained with reinforcement learning for reasoning \citep{yang2025qwen3}. \\
Vicuna              & 7B, 13B & \multicolumn{2}{l}{—} & LLaMA fine-tuned on ShareGPT conversations; widely used as VLM language backbone \citep{zheng2023vicuna}. \\
\bottomrule
\end{tabularx}
\end{table*}

\section{Abstention}
\label{app:abstention}

\subsection{Abstention Classifier Details}
\label{app:abstention_classifier}

\input{tables/abstention_keyword_table}

Hard abstention = explicit refusal; soft abstention = non-substantive placeholders (\textit{``nothing''} and hedged yes/no excluded). Table~\ref{tab:abstention_keywords} lists all keyword categories.

\subsection{Abstention by Question Type}
\label{app:abstention_by_qtype}

Per-model abstention rates by question type and condition (see \S\ref{sec:setup} for filtering criteria).

\input{tables/abstention_by_qtype}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{confidence/abstention_vs_confidence_hist_inst_blind}
\caption{Token confidence (mean token probability) pooled across all model groups and scales (VLMs, backbone decoders, standalone LLMs, and thinking models; 0.6B--13B), instruction-blind condition ($N{=}339$ question--variant pairs). \textbf{Top:} committed vs.\ abstained answers. Abstained answers are markedly lower in confidence, confirming abstentions reflect genuine uncertainty rather than high-confidence refusals. \textbf{Bottom:} soft vs.\ hard abstention split. Soft abstentions show higher confidence than hard abstentions, consistent with partial commitment rather than complete uncertainty.}
\label{fig:abstention_confidence_hist}
\end{figure}

\iffalse % Token Confidence Distributions — removed (not tied to main paper claims)
\clearpage
\section{Token Confidence Distributions}
\label{app:uncertainty_appendix}

\iffalse % Confidence Alignment by Architecture — removed (not discussed in main paper)
\subsection{Confidence Alignment by Architecture}
\label{app:confidence_alignment}

Human--model confidence correlation (Pearson $r$ between mean human self-reported confidence and mean model token log-probability) reveals a structural dissociation: backbone decoders and standalone LLMs correlate most strongly with human uncertainty (backbone: $r{=}0.38$--$0.39$; standalone LLM: $r{=}0.41$ on original questions), while VLMs are consistently weakest ($r{=}0.13$, $0.13$, $0.04$ across variants) despite strong blind accuracy---confirming that a model can answer benchmark-favorably without tracking which questions humans find hard.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{hh_hm_grouped_scatter_7b_all_variants/7b_confidence_op_ent_abstfiltered}
\caption{Human vs.\ model confidence grouped by operation type (left) and entity type (right), abstention-filtered. Confidence is visibly flatter than SBERT and accuracy, with weaker operation-type structure across all groups.}
\label{fig:scatter_confidence}
\end{figure*}
\fi % end confidence alignment block

\subsection{Model Token Confidence Distribution}
\label{app:confidence_ecdf}

ECDF of mean token log-probability per answer for matched 7/8B models (instruction-blind, $N{=}339$).

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{confidence/grouped_7b}
\caption{ECDF of mean token log-probability per answer. Rows = model group; columns = individual 7B/8B models (backbone decoder titles omit ``(LM)'').}
\label{fig:ecdf_confidence_7b}
\end{figure*}

\fi % end token confidence distributions block

\clearpage
\section{Instruction Effects}
\label{app:inst}
\label{app:abstention_calibration}
\label{app:abstention_full}
\label{app:abstention_scale_full}

Blind accuracy, HM SBERT, and abstention by group with instruction deltas (see \S\ref{sec:dist_alignment} for discussion; per-model abstention by question type in Appendix~\ref{app:abstention_by_qtype}).

% js_instruction_effect table removed: Δ now shown inline in Table~\ref{tab:hm_grouped_distribution_7b_compact_filtered} in the main paper.

\input{tables/inst_effect_combined}

\label{app:yesno_dist}
Table~\ref{tab:yesno_distribution_all} shows the full yes/no answer distribution for all models across all scales (see \S\ref{sec:dist_alignment} for discussion of the 7/8B matched subset).

\input{tables/yesno_distribution_all_models}

\label{app:count_dist}
Figure~\ref{fig:count_transition_all} extends the InternVL-8B count-distribution view to all 7/8B models (see \S\ref{sec:dist_alignment}).

\begin{figure*}[ht]
\centering
\includegraphics[width=\textwidth]{answer_distribution/transition/vABC_all_models}
\caption{Count-answer distributions for all 7/8B models across all three variants (pooled). Row 1: full VLMs; Row 2: backbone decoders; Row 3: standalone LLMs and thinking models. Hollow = blind, filled = +instruction. The zero-default collapse in full VLMs is consistent across all families (LLaVA-Vicuna: 98\% zero, unchanged under instruction). Among standalone LLMs, Qwen3-8B is most responsive to instruction (zero-default drops from ${\sim}$49\% to ${\sim}$26\%); Mistral-7B remains near zero due to its high abstention rate. Backbone decoders show partial recovery, less than standalone LLMs but more than their paired VLMs.}
\label{fig:count_transition_all}
\end{figure*}

\clearpage
\section{Alignment}
\label{app:model_diagnostics}
\label{app:metrics}
\label{app:scale_variant}
\label{fig:scale}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/subject_correlation_matrix}
\caption{Pairwise mean SBERT matrix for all 77 subjects (40 humans + 37 models), original questions ($N{=}113$). Each cell is the mean SBERT score across all 113 questions for that subject pair; question--subject pairs where either participant abstained are excluded before computing scores. The Human$\times$Human block shows moderate within-group agreement; VLM$\times$VLM and VLM$\times$Backbone blocks are high (models within a family give similar answers); the two near-white columns in SA-LLM correspond to Mistral-7B, whose verbose outputs yield near-zero pairwise SBERT with all other subjects.}
\label{fig:subject_corr_matrix}
\end{figure}

\subsection{Human--Model Alignment: Family-Level Tests}
\label{app:degradation}
\label{app:family_alignment_tests}

\input{tables/matched_family_alignment_tests}

\subsection{Anchoring Degradation by Group}
\label{app:anchoring_degradation}

\input{tables/anchoring_degradation}

\subsection{InternVL-8B: Per-Group Anchoring Excess}
\label{app:internvl_anchoring}

Table~\ref{tab:internvl_anchoring} shows per-group anchoring excess for InternVL-8B (VLM vs.\ backbone decoder).
The VLM has positive excess (under-degrades relative to humans) on visual entity groups---\textit{Place} ($+0.104$), \textit{Vehicle} ($+0.060$), \textit{Animal} ($+0.023$)---while the backbone over-degrades on the same groups.
This reveals that InternVL's encoder selectively enriches \emph{visual} entity priors, making the VLM less anchor-dependent than its backbone on those specific groups.
In contrast, both VLM and backbone over-degrade on \textit{Identity} and \textit{Spatial} operations, where the referent anchor is irreplaceable.

\input{tables/internvl_anchoring}

\subsection{Anchoring Degradation by Group and Architecture Class}
\label{app:delta_bar}

\begin{figure*}[ht]
\centering
\includegraphics[width=\textwidth]{figures/hh_hm_grouped_scatter_7b_all_variants/7b_sbert_delta_bar_abstfiltered}
\caption{Orig.$\to$Pron.\ degradation by operation type (top row) and entity type (bottom row) for SBERT (left column) and exact-match accuracy (right column).
Low-frequency operation groups (Causality, Comparison, Temporal, Text Reading, World Knowledge; $n{\leq}5$) and entity groups (Place, Product, Vehicle, Text) are merged into \textit{Other}, matching the main-paper grouping.
\textbf{Bars} = per-architecture-class mean $\Delta$ (VLM = blue; Backbone = green; SA-LLM = red); \textbf{markers} = individual model $\Delta$ (shape encodes model family; see top legend); \textbf{error bars} = 95\% CI across models within class.
\textbf{Black diamonds} = human (HH) $\Delta$; groups ordered by human SBERT $\Delta$ ascending (left-most = most anchor-sensitive), with order held constant across both metric columns.
Bars falling below the human diamond indicate model over-degradation relative to the human baseline.
Accuracy and SBERT patterns are broadly consistent, confirming that the anchoring effects observed under SBERT are not metric-specific.
\textit{Note:} Spearman $\rho$ values in Figure~\ref{fig:variants} are computed over all unmerged groups (12 op / 9 ent) for maximum statistical power; the merged grouping here serves visual clarity only.}
\label{fig:delta_bar}
\end{figure*}

\subsection{Per-Model Alignment Statistics}
\label{app:per_model_r}

Table~\ref{tab:per_model_r} reports Spearman $\rho$ between HH and HM SBERT over group$\times$variant means ($n{=}36$ op; $n{=}27$ ent) for each model, measuring how closely models track the human group-level response pattern across all three variants.
Operation $\rho$ is high across most VLMs and backbone decoders (0.85--0.96), with Mistral-7B as the sole negative outlier ($\rho{=}{-}0.591$) due to verbose refusals.
Entity $\rho$ is more variable: backbone decoders are generally consistent (0.71--0.82), while some VLMs show weaker entity-level concordance (LLaVA-1.5: 0.540).

\input{tables/per_model_r_table}

\subsection{SBERT Length-Normalization Robustness}
\label{app:chrf_human}

\begin{figure*}[t]
\centering
\begin{subfigure}[b]{0.48\linewidth}
\includegraphics[width=\linewidth]{figures/appQ_sbert_robustness/sbert_truncation_scatter_vc_filtered}
\caption{Abstention-filtered}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.48\linewidth}
\includegraphics[width=\linewidth]{figures/appQ_sbert_robustness/sbert_truncation_scatter_vc_with_abst}
\caption{With abstentions}
\end{subfigure}
\caption{Raw vs.\ 5-word-truncated HM SBERT per model (original variant, instruction-blind). Each point is one model; colors indicate model group; dashed line = $y{=}x$. \textbf{(a)} After abstention filtering, all groups cluster tightly on the diagonal. \textbf{(b)} Including abstentions, think models shift above the diagonal as truncation removes verbose reasoning traces---motivating filtered scores as the primary metric.}
\label{fig:sbert_truncation_scatter}
\end{figure*}

Human--human agreement by operation and entity type across four metrics (SBERT, chrF, ROUGE-1, Jaccard). Surface metrics show a flat profile vs.\ the SBERT gradient in \S\ref{sec:human_patterns}.

\input{tables/hh_metric_robustness}

\subsection{Per-Model Agreement Across Metrics}

\input{tables/comprehensive_inst_blind_table}

\subsection{Accuracy Degradation and Human--Model Correlation}
\label{app:accuracy_robustness}

The operation-type structure found in SBERT alignment also holds under blind VQA accuracy, confirming it is not an artefact of the semantic metric.
% Blind VQA accuracy and HM SBERT capture related but dissociable aspects of model behavior.
% Backbone decoders score higher than their paired full VLMs on both accuracy and SBERT for most families, consistent with the vision encoder suppressing the language prior on uninformative input.
% However, scaling within a family increases blind accuracy without reliably improving SBERT (e.g., Qwen3-VL 2B$\to$8B: accuracy rises monotonically while backbone SBERT peaks at 4B), indicating that stronger benchmark performance does not imply a more human-like prior distribution.
% InternVL is the notable exception: its full VLM leads its backbone decoder on SBERT (0.569 vs.\ 0.518, variant A) while remaining comparable on accuracy (both 0.197).

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{degradation_by_ent_op_7b_inst_blind/accuracy/combined}
\caption{Matched 7/8B instruction-blind accuracy degradation profiles across control variants
(Orig.$\to$Weak.$\to$Pron.). Top row: by operation type; bottom row: by entity type.
The operation-type pattern mirrors the SBERT result: identity and spatial questions collapse
most under pronominalization, while count and action questions remain relatively robust.
Bold lines show group means; thin lines show individual models.}
\label{fig:combined_accuracy}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{hh_hm_grouped_scatter_7b_all_variants/7b_accuracy_op_ent_abstfiltered}
\caption{Human vs.\ model blind accuracy grouped by operation type (left) and entity type (right), abstention-filtered. Each panel reports Pearson $r$ and $p$ over the grouped means. The same family ordering appears under accuracy, but operation grouping remains the more separating structural axis.}
\label{fig:scatter_accuracy}
\end{figure*}

\clearpage
\section{Image-Condition Ablation}
\label{app:ablation}

Alignment metrics under four uninformative image conditions (see \S\ref{sec:ablation} for discussion).

\begin{table}[h]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{llcccc}
\toprule
\textbf{Model} & \textbf{Image} & \textbf{Acc.} & \textbf{SBERT} & \textbf{Abst.} & \textbf{Chg.} \\
\midrule
\multirow{4}{*}{Qwen3-VL-8B}
 & Black & \underline{0.342} & \underline{0.614} & 4.5\% & --- \\
 & Gray  & 0.310 & 0.574 & 10.9\% & 38.1\% \\
 & Noise & 0.262 & 0.490 & 28.4\% & 51.0\% \\
 & White & \textbf{0.371} & \textbf{0.628} & 2.2\% & 26.2\% \\
\midrule
\multirow{4}{*}{LLaVA-Mistral}
 & Black & 0.344 & 0.609 & 2.9\% & --- \\
 & Gray  & \underline{0.363} & \underline{0.622} & 2.5\% & 22.4\% \\
 & Noise & 0.353 & 0.590 & 3.0\% & 27.6\% \\
 & White & \textbf{0.373} & \textbf{0.626} & 2.4\% & 24.9\% \\
\midrule
\multirow{4}{*}{InternVL-8B}
 & Black & \underline{0.372} & \underline{0.658} & 0.0\% & --- \\
 & Gray  & 0.365 & 0.648 & 0.1\% & 21.6\% \\
 & Noise & 0.349 & 0.639 & 0.3\% & 45.1\% \\
 & White & \textbf{0.379} & \textbf{0.660} & 0.0\% & 18.8\% \\
\bottomrule
\end{tabular}
\caption{Image-condition ablation (original question variant, 1{,}000-question blind set).
Chg.\ = response change rate relative to the black image baseline.
\textbf{Bold}: best Acc/SBERT within model; \underline{underline}: second best.}
\label{tab:image_ablation_main}
\end{table}



\clearpage
\section{Does More Thinking Help?}
\label{app:thinking}
\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{think_eda/think_abs_sbert_accuracy_horiz}
\caption{Qwen3 thinking models vs.\ mean reasoning-trace length. \textbf{(a)} HM SBERT; gray band and dashed lines mark the human reference. \textbf{(b)} Blind VQA accuracy; gray band marks the human reference. \textbf{(c)} Mean answer log-probability. Filled markers = blind-with-instruction; open = blind-only.}
\label{fig:think_abs_vs_length}
\end{figure*}

Table~\ref{tab:thinking_wilcoxon} reports paired Wilcoxon tests comparing
thinking and non-thinking HM SBERT at each matched Qwen3 size.
The benefit of thinking is confined to 1.7B ($\Delta{=}{+}0.110$, $p{<}.001$),
where the non-thinking model is substantially weaker; at 0.6B there is no
significant difference ($p{=}.317$). The 4B and 8B configurations share
the same checkpoint between thinking and non-thinking modes and are excluded.
The pooled effect across testable sizes ($\Delta{=}{+}0.048$, $p{<}.001$)
is driven largely by the 1.7B outlier.
Longer reasoning traces correlate with higher final-answer token confidence
($r{=}0.26$, $p{<}.001$, $n{=}93{,}021$ question--variant observations),
consistent with overconfidence under extended reasoning budgets.

\input{tables/thinking_wilcoxon}

% Table~\ref{tab:think_stats} and Figure~\ref{fig:think_abs_vs_length} give the full per-size breakdown.
% Neither trace length nor trace presence predicts alignment monotonically.
% On count questions, thinking under instruction shifts aggregate answer distributions toward human-like small counts, yet at the question-specific level the shift often remains misaligned: the model moves from ``0'' to a plausible count such as ``1'' or ``3'' while the human majority concentrates on a different value, so gains in marginal distribution do not translate into higher HM SBERT.
% \input{tables/think_consolidated}
% cut: per-size detail already covered by thinking_wilcoxon + fig:think_abs_vs_length


% ─── COMMENTED OUT (app:aggregation): not referenced from main body ───
\iffalse
\clearpage
\section{Answer Aggregation}
\label{app:aggregation}

We cluster semantically equivalent answers (e.g., ``3'' and ``three'') using
GPT-4o mini \citep{openai2025gpt4o}. Human responses are aggregated into a
confidence-weighted distribution:
\begin{equation}
H(a_k^*) = \frac{\sum_{i:\phi(a_i)=a_k^*} c_i}{\sum_i c_i}
\end{equation}
where $c_i$ is participant $i$'s Likert confidence (normalized to $[0,1]$)
and $\phi$ maps raw answers to canonical forms.

\fi
% ─── END COMMENTED OUT ───
% ─── COMMENTED OUT (app:family_alignment): not referenced from main body ───
\iffalse
\clearpage
\section{Per-Family Alignment Structure}
\label{app:family_alignment}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{variants_ladder_family_sbert_inst_blind/sbert_family_vABC_q113_yesno_raw}
\caption{Per-family SBERT human--model agreement across control variants (raw, unfiltered).
Row~1: VLMs (solid) and backbone decoders (dotted).
Right column: group-mean summary with 95\% CI. Line weight scales with parameter count.}
\label{fig:family}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{variants_ladder_family_sbert_inst_blind/sbert_family_vABC_q113_yesno_abstfiltered}
\caption{Per-family SBERT human--model agreement (abstention-filtered).
Same layout as Figure~\ref{fig:family} but pairs where either answer is a hard or soft abstention are excluded.
Filtering primarily removes model responses that abstain before comparison against human responses; the family-level trend is robust across both views.}
\label{fig:family_filtered}
\end{figure*}

Figure~\ref{fig:family} decomposes the group-level trend into individual
model families. In Row~1, backbone decoders consistently match or exceed
their paired VLMs within each family, with the gap widest for InternVL
(backbone 8B exceeds VLM 8B by $+$0.03--0.05 SBERT). In Row~2, Qwen3 standalone LLMs show clear scale
ordering (0.6B at bottom, 4--8B at top), while Mistral-7B is
a persistent outlier with the lowest agreement of any model ($\sim$0.18--0.25),
consistent with its high verbosity (mean 29.7 words per answer).
Mistral-7B achieves 0\% VQA accuracy across all $N{=}339$
responses, yet this is partly a scoring artifact of its verbosity rather
than a complete failure to exploit priors.
Although approximately 48\% of its responses contain plausible
prior-based guesses (e.g., ``baggage claim area\ldots{} where luggage
is retrieved after arriving at airport'' for GT: \textit{airport};
``ketchup and mustard are commonly used condiments'' for GT:
\textit{mustard, ketchup}), these answers are embedded in
multi-sentence elaborations (97\% of answers exceed 30 words) that
prevent VQA-standard matching from extracting them.
The remaining 52\% are explicit refusals.
It is therefore excluded from the variant ladder plots in
Figure~\ref{fig:scale_variant}, where it would appear as a
flat line at zero.

\fi
% ─── END COMMENTED OUT ───
% ─── COMMENTED OUT (app:sbert_robustness): not referenced from main body ───
\iffalse
\clearpage
\section{SBERT Metric Robustness}
\label{app:sbert_robustness}

\subsection{Alternative Embedding Models}
\label{app:embed_compare}

Table~\ref{tab:robustness_embedding} compares group-level human--model alignment
under three embedding metrics: our primary SBERT (all-mpnet-base-v2), a
supervised contrastive model (SimCSE; sup-simcse-roberta-large), and BERTScore~F1.
The backbone~$>$~VLM~$>$~SA-LLM ordering is reproduced under all three embeddings.
Spearman~$\rho$ between individual model rankings under SBERT and SimCSE is 0.961,
confirming that the group-level finding is not an artefact of the choice of
embedding model. BERTScore exhibits a compressed dynamic range across groups
(0.912--0.948), making it less discriminative, but preserves the same
group ordering.

\input{tables/robustness_embedding_compare}

\subsection{Pairwise Metric Correlations}
\label{app:metric_corr}

Figure~\ref{fig:metric_corr} shows pairwise Pearson correlations between all
seven evaluation metrics at the per-question$\times$variant aggregation level.
SBERT and SimCSE are tightly coupled ($r{=}0.87$), confirming that both
capture the same underlying semantic signal.
Lexical metrics (ChrF, ROUGE-1, Jaccard, Exact) form a separate cluster
with much weaker correlation to the embedding-based metrics ($r{\approx}0.3$--$0.5$),
consistent with the null chrF result in Appendix~\ref{app:chrf_human}.

\begin{figure}[t]
\centering
\includegraphics[width=0.72\columnwidth]{appQ_sbert_robustness/metric_correlation_per_question_hm_113}
\caption{Pairwise Pearson~$r$ between evaluation metrics at the
per-question$\times$variant mean level ($n{=}113$ questions, model-to-human response comparisons).
SBERT and SimCSE form a tight cluster ($r{=}0.87$); lexical metrics form a
separate cluster. Lower triangle shown; diagonal is 1.}
\label{fig:metric_corr}
\end{figure}

\subsection{Group Rankings Across Operation Types}
\label{app:op_group}

Table~\ref{tab:robustness_op_group} breaks down mean HM SBERT by operation
type and model group. The backbone~$\geq$~VLM advantage holds for every one
of the eleven operation types, including existence ($\Delta{=}+0.067$) and
action ($\Delta{=}+0.046$). SA-LLMs fall furthest below the HH reference
across all operation types.

\input{tables/robustness_op_group_sbert}


\fi
% ─── END COMMENTED OUT ───
